# 2026-07-11 스튜디오 채널 메뉴 (UX/UI) — 채널 자산·규격 관리 마스터

> 압축본: `_os/charter/decisions-index.md` (D-064)
> 결정자: 대표님 / 정리: Claude
> 스튜디오 6메뉴 중 여섯 번째 "채널". 라이브 코드(channels·channel_cid_map·youtube.js·_lib/youtube-rules.js·_content/youtube/*.md) 검증 기반.

## 목적
채널 메뉴 = **콘텐츠를 올리는 "채널 자산"과 "채널별 규격"을 한곳에서 등록·관리하는 마스터**. 채널의 이름·언어·플랫폼·아고다 CID·활성·순서 + **자동 생성 규격**(제목·파일명·본문·키워드·나레이션)을 관리한다. 스튜디오의 올리기·성과표·호텔이 전부 이 채널 위에서 돈다.
- **경계**: 성과표 "채널별"=채널 성과 보기 / 채널 메뉴=채널 자체를 등록·설정·CID·규격 관리.

## 2층 구조
1. **채널 기본 레지스트리** — 코드·이름(한/영)·언어·플랫폼·아고다 CID(다수, old/new)·활성(is_active)·표시순서(display_order). → `channels` + `channel_cid_map`.
2. **채널 규격** — 자동 생성 규칙 §1~§5. → 문서 `_content/youtube/{채널}.md`.

## 규격 항목 (라이브 호텔이야.md v1 기준)
- §1 제목: 슬롯 순서(지역→도시→특징)·글자수(60자 이내)·제목 해시태그 없음·성급 처리(3성 이하 제목 제외).
- §2 파일명: 형식·주키워드 5~6개·확정 제목으로 덮어쓰기.
- §3 본문(더보기) 블록 순서: 요약 → 호텔블록 → 조사기준 → 챕터 → 한눈에 → 고지 → 해시태그 10개. **링크=TOP1 맨 위 / 챕터=TOP3 맨 위(전 채널 통일)**. 숏폼은 챕터 없음.
- §4 키워드(태그란 500자): 슬롯 템플릿·지역 키워드 40%.
- §5 오프닝 나레이션: 언어=채널 언어·톤.

## 구현 방식 = A안 (확정)
규격을 **화면 폼으로 편집 → 문서 저장(github-commit)**. render(youtube.js)는 지금처럼 그 문서를 읽음 → 자동 생성·전 메뉴 적용에 바로 연결. 개발 최소, 지금 작동 유지.
- **필수 개조 1건(기술, 클로드 자율)**: `_lib/youtube-rules.js`의 하드코딩 `CHANNELS=['여행능력자들','호텔이야','호텔이곳']` 배열과 `detectChannel` 하드코딩 제거 → **채널 DB 기반**으로. 채널 레지스트리를 돌며 각 채널 규격 문서를 읽고, 채널 판별도 DB 이름/CID로. 그래야 새 채널이 **코드 수정 없이** 자동 인식·생성.
- 이 개조로 2층이 이어짐: 기본(DB)=드라이버(어떤 채널이 있나), 규격(문서)=내용(어떻게 뽑나).

## 새 채널 규격 만들기 = Claude 대화로 작성 → .md 등록 → 자동 통합 (확정 · 심플화)
스튜디오 안에 위저드를 만들지 않는다. 규격을 만드는 어려운 부분은 **이미 잘 되는 Claude 대화창**이 맡는다.
1. **Claude 대화창에서 작성** — 샘플 워드 파일을 Claude 채팅에 넣고 대화로 정리(제목·본문·태그·키워드 하나씩) → 최종 `{채널}.md` 산출. (기존 3채널을 만든 그 방식 그대로)
2. **채널 메뉴에서 .md 등록** — 완성된 `{채널}.md`를 업로드. 파일 §0 채널정보(채널명·언어)와 규격 내 아고다 링크(CID)를 자동 인식.
3. **자동 통합 (등록하면 시스템이 알아서)** — ① channels 등록 + channel_cid_map에 CID 매핑 ② `_content/youtube/{채널}.md` 배치 + 활성화 ③ render(loadRules DB기반) 인식 → 원고 올리면 즉시 자동 생성.
- 채널 메뉴 = 규격 .md **등록소 + 자동 통합**. 드래프팅·정리는 Claude 대화가 담당 → 스튜디오 UI가 훨씬 단순.
- 권한: 등록 = owner/admin만, 에디터 조회만.

## 권한 (확정)
- 채널 생성·규격 저장·CID 편집 = **owner 또는 admin만**. **에디터는 조회만**(생성·저장 불가).
- **두 겹 방어**: ① UI에서 버튼 미표시(에디터·매니저) ② 서버 API에서 역할 검증(우회 호출 거부). 버튼만 숨기면 뚫리니 서버 게이트 필수.

## CID 관리
채널별 CID 추가/폐기(신규 전환 시 old 유지·new 추가). CID = 아고다 예약↔채널 연결 키(성과표·호텔의 뿌리). CID 확정값은 대표님 화면 검증 완료(여행능력자들 유튜브 1913282·웹 1919025·호텔이야 1932026·호텔이곳 1946819).

## 전파 (단일 진실 → 각 메뉴)
규격/기본을 한곳(규격 문서 + 채널 DB)에서 고치면 → render가 읽어 **올리기(복사 4카드 자동)** · **성과표(채널명·언어 표기)** · **호텔(채널별 노출·예약)** 에 자동 반영.

## 데이터·연결
- 채널 기본 = `channels`(code·name·name_en·language·platform·has_agoda_api·agoda_site_id·is_active·display_order·notes)
- CID = `channel_cid_map`(cid·channel_code·cid_label old/new·is_active)
- 규격 = `_content/youtube/{채널}.md`, render=`_lib/youtube-rules.js`(loadRules·detectChannel)·`_lib/youtube-render.js`
- 키워드 실측 = `_content/youtube/keywords`
- 시드 채널(라이브): TW 여행능력자들·HT 호텔이야·KT Kotel·TC 호텔닷컴·JP ホテルだ·ZH 世界就是家 (+ VN·HG 등 CID)

## 후속
- loadRules DB 기반 개조 → 채널 목록·CID 추가/폐기·규격 편집·AI 초안·미리보기 세부 화면 순차 설계.

## 구현 로그

### 2026-07-12 — 1층(채널 레지스트리 + CID) 조회 화면 착수 (BL-STUDIO-MENU-6TAB)
스튜디오를 6메뉴 셸(올리기·성과표·호텔·키워드·전략·채널 상단 탭)로 개편하고, 그 중 **채널 메뉴 1층(조회)**을 라이브에 올렸다. 기존 올리기 기능(원고 넣기·채널별 원고 탭·복사 4카드·발행)은 그대로 보존.
- **신설 `api/channels.js` (GET)** — 세션(is_editor 이상) 또는 x-ops-token 인증. service_role 로 `channels` + `channel_cid_map` 을 읽어 채널별로 CID 를 묶어 반환. is_admin 플래그 포함(편집 버튼 노출 판정용). 권한 판정은 DB(is_editor/is_admin)가 함 — 화면 말 안 믿음(publications.js 원칙 계승). RLS 로 화면(anon)이 직접 못 읽는 문제를 이 창구가 해결.
- **`studio.html` 개편** — 상단 6메뉴 네비 + 뷰 전환. 채널 뷰 = 채널 카드(코드·이름·영문·운영/API 칩·언어·플랫폼·순서·사이트ID) + CID 목록(old/new/main 라벨·활성 여부·new 강조). 나머지 4메뉴는 "준비 중" placeholder. 언어는 D-065 보강대로 언어+시장 라벨(중국어 본토=간체·유튜브 밖 표기).
- **권한** — 이번엔 조회만 오픈. 채널 등록·CID 편집·규격 저장은 owner/admin 전용으로 후속(GET 외 405). 두 겹 방어(에디터엔 편집 버튼 미표시 + 서버 게이트)는 편집 구현 시 완성.
- **라이브 검증** — `/api/channels` 인증 없이 호출 → 401(정상 게이트). ops-token 호출 → 9개 채널·CID 정상 반환. CID 확정값 DB 일치 확인: TW 여행능력자들 1913282(main)·1919025(web), HT 호텔이야 1932026(new), HG 호텔이곳 1946819(main).

### 2026-07-12 — 채널 데이터 정리 (대표님 지시, db-query 실행)
코드가 아니라 DB 직접 변경이라 커밋에 안 남음 → 여기 기록(전수 추적).
- **호텔닷컴(TC) 삭제** — TC 는 트립닷컴 링크 기반이라 아고다 시스템과 무관. 대표님 지시로 시스템에서 배제. 실행 전 예약(bookings_agoda/self)·CID·발행(publications) 모두 0건 확인 → `DELETE FROM channels WHERE code='TC'` 안전 실행. 앞으로 TC 는 등록하지 않음(트립닷컴 링크 미지원).
- **호텔이야 일본(JP) → ホテルだ** — 실제 유튜브 채널명 반영. `@hoteluda`(구독 1.26천·영상 228, "世界中のホテルがオススメ！ アップロードは毎日夕方8時"). `UPDATE channels SET name='ホテルだ', name_en='HOTEL-DA' WHERE code='JP'`. (02-channels 원 시드값 복원 — phase4 가 "호텔이야 일본"으로 덮었던 것)
- 결과: 채널 **9개 → 8개** (운영 7: TW·HT·KT·JP(ホテルだ)·ZH·VN·HG + legacy 비활성 1).

### 2026-07-12 — 채널 표시 순서 = 2025년 매출순 (대표님 결정)
"매출이 많이 올라오는 채널부터" = 가장 활성화된 채널이 위로. bookings_unified 에서 2025년(booked_at 기준) 채널별 예약·매출 집계 → 매출순 display_order 재부여.
- **2025년 실적** (유효=취소 제외 / 매출=취소 제외 booking_amount_usd):
  1. HT 호텔이야 한국 — 예약 1171건(유효 646) · 매출 $157,905
  2. TW 여행능력자들 — 426건(유효 223) · $52,179
  3. JP ホテルだ — 252건(유효 112) · $29,624
  4. ZH 世界就是家·대만 — 86건(유효 33) · $9,120
  5. KT Koreahotelguide — 33건(유효 18) · $4,751
  6. VN Korea Hotel 베트남어 — 38건(유효 25) · $3,994
  7. HG 호텔이곳 — 2025 예약 0(운영중이나 실적 없음 → 실적 채널들 뒤)
  99. legacy 과거 누적 — 비활성, 맨 뒤 유지
- 순서 기준 = **매출(취소 제외) 내림차순**. KT vs VN: 매출은 KT 우세($4,751>$3,994), 유효예약은 VN 우세(25>18) → 대표님 기준("매출 많은 순")대로 KT 앞.
- ⚠️ 이 순서는 2025년 스냅샷. 아고다 예약 파일 새로 올리면 실데이터 재정리 시 순서도 갱신 가능.

### 2026-07-12 — 채널명 실명화 + 이름 변경 안전 원칙 (대표님 질문 답)
캡처로 실제 유튜브 채널명 확인 → 반영:
- ZH: "世界就是家·대만 World-hotel" → **世界就是家** (@world-hotel, 구독 320·영상 201). 시장(대만) 정보는 이름에서 빼고 language=zh-tw 필드가 담당(중복 제거).
- VN: "Korea Hotel 베트남어" → **Ở đâu đây? - Review Khách sạn ngay đây** (@reviewkhachsan, 구독 140·영상 239, reviewkhachsan.shop).
- JP 는 앞서 ホテルだ 반영 완료. 남은 확인 대상: HT(호텔이야 한국)·KT(Koreahotelguide) 실채널명 — 대표님 캡처 대기.

**최종 정리 (2026-07-12, 대표님 지시)** — 시장 표기 없이 채널명만(일본 ホテルだ 원칙 통일):
- HT: "호텔이야 한국" → **호텔이야** ("한국" 제거, name_en=HOTEL-IYA). 일본 채널이 시장표기 없이 ホテルだ 쓰듯 한국도 동일 원칙.
- VN: 실채널명 "Ở đâu đây? - Review Khách sạn ngay đây"가 너무 길어 → 영문 핸들 **reviewkhachsan** 으로(name_en 동일). 시장은 language=vi 가 담당.
- 최종 목록: 1.호텔이야 2.여행능력자들 3.ホテルだ 4.世界就是家 5.Koreahotelguide 6.reviewkhachsan 7.호텔이곳. KT(Koreahotelguide)만 대표님이 향후 타겟 확장 시 변경 예정(그때 이름만 UPDATE, code=KT 유지→데이터 그대로).

**★ 채널 이름 변경 안전 원칙 (code ≠ name — 데이터 안 꼬이는 이유)**
대표님 질문: "영어 채널(Koreahotelguide)을 나중에 타겟 넓혀 다른 나라도 소개하면 이름을 바꿀 텐데, 데이터가 꼬이지 않나?"
- 답: **안 꼬인다.** channels 의 진짜 열쇠는 `code`(TW·HT·KT·JP·ZH·VN·HG)다. 예약(bookings)·CID(channel_cid_map)·발행(publications)은 전부 **channel_code 로 연결**되고, `name`은 화면 표시용일 뿐이다(02-channels.sql: "CH 코드 기반으로 모든 예약 데이터를 채널 단위 분류").
- 그래서 이름을 바꿔도 code 가 그대로면 과거 예약·매출·CID·아고다 연결이 전부 그대로 붙어 있다.
- **증명(2026-07-12)**: ZH 이름을 "世界就是家·대만 World-hotel"→"世界就是家"로 바꾸기 전후, 유효예약 66·매출·CID 2개가 완전히 동일. 이름만 바뀜.
- **영어 채널(KT) 타겟 확장 시나리오**: "한국 호텔 가이드 → 세계 호텔 가이드"로 넓혀도 `code='KT'`·`language='en'` 유지하면 과거 실적 전부 이어짐. 이름(name)만 UPDATE 하면 끝. 데이터 재정리 불필요.
- **완전히 다른 새 채널을 새로 팔 때만** 새 code 부여 + CID 따로 등록(그때는 별개 채널로 실적도 분리 집계).
- 정리 방법(클로드): `UPDATE channels SET name=... WHERE code=...` 한 줄(db-query). 앞으로 채널 메뉴에 편집 화면 붙이면 대표님이 화면에서 직접 이름 변경 가능(후속).

### ⚠️ 구현 중 발견 — 대표님 확인 필요 (2026-07-12 갱신)
1. ~~채널 9개~~ → **8개로 정리**(TC 삭제 완료). 운영 7 + legacy(과거 누적·비활성) 1.
2. ~~표시 순서 뒤죽박죽~~ → **해결(2026-07-12)**: 2025년 매출순 정렬(HT>TW>JP>ZH>KT>VN>HG, legacy 맨 뒤). 대표님 결정.
3. ~~채널명 시장 혼재~~ → **해결(2026-07-12)**: HT=호텔이야·JP=ホテルだ·ZH=世界就是家·VN=reviewkhachsan 정리 완료(시장표기 제거, language 필드가 담당). KT=Koreahotelguide 는 향후 타겟 확장 시 대표님이 이름 변경 예정.
4. ~~channel_cid_map.notes 컬럼 없음~~ → API 에서 제외 처리 완료(메모 기능 필요 시 컬럼 추가 후속).

### 2026-07-12 — 채널 편집 화면 구현 (개발 순서 2/3, 클로드 자율 판단)
개발 순서는 클로드가 정해 차례 진행(헌법 부칙 16). 순서: ①조회(완료) → ②기본정보 편집(이번) → ③CID편집·신규등록 → ④youtube-rules.js DB개조(위험·맨뒤).
- **api/channels.js PATCH 추가** — 채널 이름(name)·영문명(name_en)·표시순서(display_order)·운영여부(is_active)·언어(language) 수정. **권한=admin/owner만**(who.isAdmin 아니면 403), 두 겹 방어(에디터엔 UI 버튼 미표시 + 서버 게이트). **code(예약·CID 연결 열쇠)는 화이트리스트에서 제외 → 변경 불가**(데이터 안 꼬임 보장). 화이트리스트 필드만 UPDATE.
- **studio.html 채널 카드에 [수정]** — 관리자일 때만 각 카드에 [수정] 버튼 → 인라인 폼(이름·영문명·순서·운영중 토글)→[저장](PATCH)→전체 재렌더(순서 즉시 반영). 조회 전용 사용자(에디터)는 버튼 안 보임.
- **라이브 검증**: 인증 없이 PATCH→401 / ops-token PATCH→정상 저장 / code 없이→400 방어. 통과.
- 지금까지 클로드가 db-query로 직접 하던 채널 이름·순서 변경을 **대표님이 화면에서 직접** 할 수 있게 됨.

### 후속 (다음 채팅 격리)
- `youtube-rules.js` 하드코딩 CHANNELS·detectChannel → DB 기반 개조(D-064 필수 개조 1건). 라이브 원고 생성 파이프라인을 건드리므로 위험 격리하여 별도 진행.
- 채널 등록/CID 편집/규격(.md) 등록·자동 통합 화면(owner/admin 전용).
- display_order·채널명·언어+시장 정리(대표님 결정 후).

## 보강 (2026-07-12) — 언어를 "언어+시장"으로 확장 (D-065 파급)
- 채널 등록 시 언어를 단일 "중국어"가 아니라 **"언어+시장"**으로 구분 입력(예: 중국어(대만·번체) / 중국어(홍콩·번체) / 중국어(중국본토·간체)). 키워드 메뉴 타겟 축(D-065 보강)이 이 값을 받아 타겟을 자동 생성한다. 본토=유튜브 밖 표시.
- 근거: 같은 언어라도 시장별 검색어·플랫폼·유튜브 접근성이 다름(번체 vs 간체). channels 레지스트리 언어 필드 = 언어+시장.

---

## 구현 로그 — 단계5(CID·신규등록) 완료 · 2026-07-12

**커밋**: api/channels.js `08545be` + studio.html `40f4fba` [step:done:5]

**한 것**: 채널 API에 POST 추가(관리자 전용, 두 겹 방어) — `create_channel`(새 채널 기본 등록) / `add_cid` / `retire_cid` / `restore_cid`. 화면엔 [＋ 새 채널] 등록폼 + 채널별 [폐기]·[복구]·[＋ CID 추가]. **CID는 하드삭제 금지 — 폐기=is_active=false**(헌법 9, 과거 예약 집계 보존). 라이브 검증: 무토큰 401 · 없는채널 404 · 필드누락 한국어 400 정상.

**대표님 승인 결정(2026-07-12)**: "새 채널 등록"을 두 겹으로 나눠 진행 —
- **지금(단계5)**: 기본 레지스트리 **수동 폼**(코드·이름·언어·첫 CID)로 등록.
- **단계6 이후**: 규격 `.md` 업로드 → 자동 통합(§0 채널정보·아고다 CID 자동 인식 + `_content/youtube/{채널}.md` 배치 + render 인식)은 `youtube-rules.js` DB개조(단계6, ⚠️라이브 파이프라인 위험작업)를 마친 뒤 연결. 단계6은 새 채팅에서 진행.

---

## 정정 (2026-07-12) — 단계5 새 채널 등록 방식

**변경:** 새 채널 등록을 **수동 입력 폼 → 규격 `.md` 업로드 방식**으로 교체 (D-064 원안 복원).

- **근거:** 규격 md(`_content/youtube/{채널}.md`) §0 채널 정보에 채널명·언어·아고다 CID 가 이미 박혀 있음 → md 하나만 올리면 자동 등록. 사람이 같은 정보를 두 번 타이핑할 필요 없음(단일 진실).
- **예외:** 채널 **코드**(예약 연결 열쇠·불변)는 md 에 없으므로 등록 시 사람이 1개만 확인 입력. 나머지(이름·언어·CID)는 서버가 md §0 에서 자동 파싱.
- **흐름:** 코드 입력 → [파일 읽기](서버 `dry_run` 파싱 미리보기) → 확인 → [등록].
- **구현:** `api/channels.js` `action=register_from_md` (파싱 규칙 서버 단일 진실, `dry_run` 지원) / `studio.html` 폼 교체. 기존 `create_channel` 은 가역성 위해 API 에 잔존(UI 노출만 제거).
- **검증:** 실제 규격 md 3종(호텔이야·여행능력자들·호텔이곳) 파싱 3/3 통과 — 이름·언어·CID 정확, CID 확정값 일치. 라이브 인증 게이트 정상.
- **커밋:** api/channels.js `5bdcd64` / studio.html `a983fdc`

---

## CID 상태 원칙 (2026-07-13 추가 · 혼동/중복 방지 — 서비스 운영 실체)

> 라이브 데이터로 검증한 CID 실체. 채널 메뉴 화면·성과표·호텔이 모두 이 위에서 돈다.

### 사실 (라이브 검증)
- **한 채널에 CID 여러 개 = 정상.** 아고다 계정(아이디)이 여럿이라, 계정마다 같은 채널의 고유번호를 따로 발급받았다. (예: 호텔이야 = new 1932026 · old 1922821 · approval-2nd 1931762)
- 예약 7,169건 **전부**가 정식 등록 CID(`channel_cid_map` 14개) 안에 있고, 채널 배정도 **100% 일치**(명단 밖·엉뚱한 채널 0건). → 잘못 발급 없음.
- **old 번호는 죽지 않았다.** 과거 발행 영상에 old 링크가 박혀 있어 지금도 예약이 들어온다:
  - 호텔이야 old(1922821) = 예약 1,188건, 최근 2026-03-15 (new와 거의 같은 시기)
  - 베트남 old(1922822) = 최근 2026-03-30 으로 **new(2025-12-11)보다 더 최근**

### 원칙 (헷갈림 방지 — 두 가지를 분리한다)
1. **발급 세대 딱지(old/new/main/web/approval-2nd)** = 어느 계정·시기·경로에 만든 번호인지 **이력 표시일 뿐.** 좋고 나쁨/유효·무효가 아니다.
2. **용도는 두 가지로 갈린다:**
   - **예약 집계** → old·new **전부 계속 사용.** 예약이 들어오면 이 번호로 어느 채널인지 센다. old도 집계에서 절대 빼면 안 된다(빼면 예약이 "채널 불명"으로 유실).
   - **신규 링크 발급** → 보통 **new/main만** 새 영상에 쓴다. old는 새로 안 쓰지만(신규 발급 중단), 예약 집계는 계속.

### "폐기" 용어 폐기 (중요)
- 화면의 **[폐기] 표현은 오해를 부른다.** old = "죽은 번호"가 아니라 **"새 링크엔 안 쓰지만 예약은 계속 들어오는 살아있는 번호."**
- 화면 표시를 상태 배지로 교체: `현재 발급`(new/main) / `예전 번호 · 예약 집계중`(old 등, 초록불 유지).
- CID를 목록에서 **정말 제거**하는 것은 = **잘못 등록한(우리 것이 아닌) 번호일 때만**, 경고 후 owner 전용. 현재 그런 CID는 0건.

### 연동
- 이 원칙은 D-064(채널 메뉴)·D-063(성과표 채널별)·호텔 메뉴가 공유. CID = 예약↔채널 연결 열쇠(단일 진실 `channel_cid_map`).
- 후속: 채널 메뉴 화면 "폐기" 라벨 → 상태 배지로 교체(별도 작업).


---

## 🆕 새 채널 후보 추천 (2026-07-15 대표님)

**대표님**: "채널의 추천은 채널 페이지에서 넣는 게 맞지 않나? **후에 새로운 채널을 만들 때 참고할 수 있도록.**"

### 무엇을 보여주나
**채널이 없는데 이미 예약이 나는 시장** — 남의 언어 영상을 보고 산 사람들. 채널을 만들면 더 살 수 있다는 신호.

**실측 (2026-07-15 · 아고다 원본 7,316건 · `customer_country` = 손님이 접속한 아고다 사이트 국가)**

| 후보 | 예약 | 지금 어느 채널로 들어오나 |
|---|---|---|
| **태국어 · 태국** | **65건** | 영어·일본어·한국어·베트남어 |
| **말레이어 · 말레이시아** | 25건 | 영어·일본어·한국어·번체 |
| **인도네시아어 · 인도네시아** | 11건 | 영어·한국어 |

**추천에서 제외**: 미국 72 · 필리핀 34 · 호주 32 · 싱가포르 26 · 캐나다 19 → **영어권**이라 "영어 · 전세계" 타겟이 이미 먹고 있음. 중복 추천 안 함.

### ⚠️ 이 추천의 한계 (화면에 [왜?]로 같이 보여줄 것)
**우리 영상을 본 사람만 잡힌다.** 우리가 안 판 시장은 영원히 0으로 뜬다(D-065 ⑥과 같은 함정 — "전환은 순위에 안 씀"). 즉 이 추천은 **"이미 새는 물"만 보여주지, "안 판 시장"은 못 본다.** 참고용이지 결정 근거가 아니다.

### 규칙
- 채널 메뉴에만 둔다. **키워드 메뉴에는 넣지 않는다**(D-065 ㉓ — 타겟은 채널이 만든다).
- 채널을 등록하면 → 키워드 타겟 목록에 **자동으로 생긴다**(D-064 계승). 그때부터 조사가 돈다.
- 숫자는 손으로 쓰지 않는다. `bookings_agoda.customer_country` × `channels.language` 로 매달 자동 산출.
