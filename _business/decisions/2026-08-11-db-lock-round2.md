# DB 잠금 2차 (2026-08-11) — 표 12개 추가 잠금

> **계기**: 2026-08-04 1차 잠금(27개) 이후 새로 만든 표들이 다시 열려 있었다.
> 사업 확장을 논의하다 실측했더니 **76개 중 24개가 안 잠겨 있었다.**

## 무엇이 열려 있었나 (2026-08-11 실측)

| 표 | 위험 | 조치 |
|---|---|---|
| `hotel_master` 69,803 · `hotel_pool` · `harvest_queue` | 자산 자료 유출 | ✅ 잠금 |
| `collect_log`·`demand_city`·`demand_rank`·`demand_season` | 조사 자료 | ✅ 잠금 |
| `hotel_brand`·`kr_outbound`·`screen_sweep_log`·`hotel_pool_run` | 내부 자료 | ✅ 잠금 |
| `hotels_agoda_backup_20260811` | 🔴 **오늘 클로드가 만들고 안 잠금** | ✅ 잠금 |
| 🔴 **`blog_subscriber`**(구독자 이메일) + `blog_*` 11개 | **개인정보 — 8/4 사고와 동일 유형** | ⏳ **미조치** |

## 🔴 남은 것 — `blog_` 12개

대표님 지시(*"blog_ 로 시작하는 표는 절대 건드리지 마라"*)에 따라 손대지 않았다.
블로그 화면이 익명 열쇠로 표를 직접 읽고 있으면 잠금 순간 멈추므로, **블로그 작업 창에서 확인 후 잠가야 한다.**

```sql
alter table public.blog_subscriber enable row level security;   -- 최우선(개인정보)
```

## 검증

잠금 후 `staycurate.com` 200 · `gohotelwinners.com` 200 · 창구 조회 정상.
이유 = 화면은 표를 직접 읽지 않고 항상 창구(service_role)를 거치기 때문(SYSTEM_MAP 「DB 잠금」).

## 🔴 왜 또 열렸나 — 근본 원인

**사람이 기억해서 잠그는 규칙이라 반드시 새어나간다.** 8/4에 규칙을 적었는데 3주 만에 12개가 다시 열렸다.
→ **관리실 점검판에 「안 잠긴 표 N개」를 상시 표시**한다. 0이 아니면 빨간불.

## 보안 방어선 5겹 (2026-08-11 정리)

| 겹 | 뚫리면 | 막는 법 | 상태 |
|---|---|---|---|
| 1 | 수파베이스 로그인 계정 | 2단계 인증(MFA) | ⚠️ 대표님 확인 대기 |
| 2 | `ops` 토큰 (임의 SQL 실행 가능) | 열쇠 분리·권한 축소 | ⏳ 개발 완료 후 (대표님 지시) |
| 3 | 프로젝트 자격증명 | 사업별 프로젝트 분리 | ✅ |
| 4 | 표 잠금(RLS) | 표마다 잠금 | 🟡 12/24 완료 |
| 5 | 코드·배포 | 레포·Vercel 분리 | ✅ |

🔴 **조직(Organization) 분리는 방어선이 아니다.** 로그인 계정이 하나면 조직을 나눠도 같이 털린다.
조직은 청구서 단위일 뿐 → 새 사업은 **같은 조직 안에 새 프로젝트**로 만든다(월 $25 절약).
