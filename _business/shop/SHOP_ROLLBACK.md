# SHOP_ROLLBACK — 되돌리는 법 (여행능력자들 샵)

> 대표님: *«문제가 생기면 전 단계로 돌아갈 수 있는 기록이 있어야 한다.»*
> 🔴 이 문서는 **바꾼 것마다 «어떻게 되돌리는가»를 그 자리에서 적는다.** 나중에 몰아 적지 않는다.
> 🔴 **되돌릴 수 없는 것**은 «되돌릴 수 없다»고 정직하게 적는다. 그래야 다음에 조심한다.

## 0. 되돌리는 세 가지 수단

| 무엇이 잘못됐나 | 수단 | 방법 |
|---|---|---|
| 코드·문서·설정 | **GitHub 커밋** | 모든 변경이 커밋 1개다. `github-read` 로 옛 커밋의 파일을 받아 다시 올린다 |
| 화면이 죽음 | **Vercel 이전 배포** | Vercel → 프로젝트 → Deployments → 정상이던 배포 → Promote to Production (대표님 화면) |
| 창고 자료 | **표 단위** | 아래 표의 «되돌리는 법» 대로. 표 통째로 지워도 다른 화면에 영향 없게 분리해 두었다 |

---

## 1. 2026-09-05~06 · 신상 호텔 섹션 (D-117)

### 코드·화면 — 전부 커밋으로 되돌아간다

| 파일 | 커밋 | 되돌리면 |
|---|---|---|
| `api/new.js` (신설) | `a938c65` → `f19ef81` | 파일 삭제 시 신상 칸이 사라질 뿐, 다른 화면 영향 0 |
| `public/new.html` (신설) | `52b548d` | 삭제 시 `/new/…` 주소만 사라짐 |
| `public/shop.js` | `942efdb` → `f0752cb` | 신상 부품만 뒤에 붙였다. 그 블록을 지우면 이전과 동일 |
| `public/shop.css` | `0bb8a90` → `6b65a4c` → `ada9b57` | 신상 규격만 뒤에 붙였다 |
| `public/index.html` | `2fdcfea` | `<div id="newbox">` 와 `mountNew(...)` 두 줄만 지우면 원복 |
| `vercel.json` | `e53ab75` → `d662a31` | 주소 1줄 추가 + 크론 주기 변경 |
| `api/cron/collect.js` | `b2c63fd` → `044a912` → `0b827cb` → `d5fcf55` | 🔴 **가장 조심할 파일** — 아래 참조 |

### 창고(Supabase) — 표 단위로 되돌린다

| 표·뷰 | 무엇 | 되돌리는 법 | 다른 곳 영향 |
|---|---|---|---|
| `shop_new_pool` (신설) | 신상 후보 33,659줄 | `drop table shop_new_pool` | **없음** — 신상 칸만 빔 |
| `v_shop_new` (신설) | 화면용 뷰 | `drop view v_shop_new` | **없음** |
| `shop_hotel.photo_src` (칸 추가) | 아고다 원본 사진 주소 | 칸을 두어도 무해. 지우려면 `alter table … drop column` | 없음 |
| `shop_hotel.photo_spec` (칸 추가) | 「800/2000 으로 받음」 표시 | 지우면 **사진을 전부 다시 받게 된다** — 지우지 말 것 | 🔴 있음 |

### 🔴 되돌릴 수 없는 것 (정직하게 적는다)

| 무엇 | 왜 못 되돌리나 | 다시 하려면 |
|---|---|---|
| **사진 파일** | 같은 이름(`thumb.jpg`·`big.jpg`)에 **덮어썼다**. 옛 320px·1,200px 판은 남아 있지 않다 | `photo_spec` 을 비우고 사진 창구를 다시 열어 받는다(약 70분·요금 0원) |
| **가격 기록** `shop_price_daily` | 날짜별로 «그날 잰 값»이라 되돌린다는 개념이 없다. 지우면 「소개 때 가격」 비교가 영영 불가능 | 없음 — **절대 지우지 말 것** |

---

## 2. 2026-09-06 · 수집 봇 개조 — 문제 생기면 여기부터 되돌린다

봇이 잘못되면 **가격 딱지 전체가 죽는다.** 가장 위험한 변경이므로 3단계로 되돌린다.

| 단계 | 증상 | 되돌리는 법 |
|---|---|---|
| ① 아고다가 차단(403·429) | `shop_collect_log.note` 에 `🔴차단` 이 찍힘 | `vercel.json` 크론을 `0 * * * *`(1시간)로 되돌린다 |
| ② 그래도 차단 | 계속 찍힘 | `collect.js` 의 `POOL = 4` 를 `1` 로 (동시 요청 없앰) |
| ③ 그래도 이상 | 값이 안 쌓임 | `collect.js` 를 커밋 `b2c63fd`(신상 편입 전) 으로 되돌린다. 소개 호텔 1,050곳만 수집하던 상태 |

**정상 판정 기준** — `shop_collect_log.note` 가 `cid=1919025 · 코드={"200":N}` 이고 `🔴차단` 이 없으면 정상.

---

## 3. 2026-09-05 · gohotelwinners 배포 사고 (해결됨 · 재발 방지)

| | |
|---|---|
| 증상 | 09-05 15:36 UTC 부터 **모든 배포가 Configuration error**. 빌드 로그 0줄 |
| 원인 | `vercel.json` 의 `ignoreCommand` 를 13:33 에 **제외 15개(428자)** 로 확장 |
| 어떻게 찾았나 | 커밋 시각과 실패 시각 대조 (13:33 확장 → 15:36 첫 실패) |
| 조치 | shop 에서 **실제로 배포되고 있는** 짧은 형태(70자)로 축소 → 커밋 `71ce70e` → 배포 READY 확인 |
| 🔴 재발 방지 | **`ignoreCommand` 를 늘릴 때는 한 번에 하나씩 늘리고, 늘릴 때마다 배포 성공을 눈으로 확인한다.** 이 값은 문법 검사에 안 걸리고 조용히 배포 전체를 막는다 |
| 남은 일 | 빌드 요금 절감이 원래 목적이었다. 제외 폴더를 **하나씩** 다시 늘리며 매번 배포를 확인한다 |

---

## 4. 2026-09-06 오후 · 행동 장부 · 짧은 주소 · 세부 페이지 (D-117)

### 새로 만든 것 — 지워도 손님 화면이 죽지 않는다

| 표·뷰 | 무엇 | 되돌리는 법 | 다른 곳 영향 |
|---|---|---|---|
| `shop_share` `shop_view` `shop_like` `shop_price_alert` | 행동 장부 | `drop table` | **없음** — 기록만 멈춤 |
| `shop_link` | 짧은 주소 | `drop table` | 🔴 **있음** — 이미 뿌린 `gohpik.com/h/{코드}` 가 전부 죽는다. **뿌린 뒤에는 지우지 말 것** |
| `shop_price_stat` | 미리 계산한 가격 요약 | `drop table` 후 재생성 가능(8초) | 신상 카드·세부의 가격이 사라짐 |
| `v_shop_hotel_name` 외 상세 뷰 6종 | 관리자용 | `drop view` | 없음 |

### 코드 — 커밋으로 되돌린다

| 파일 | 무엇 | 되돌리면 |
|---|---|---|
| `api/track.js` (신설) | 열람·공유·찜 기록 | 기록만 멈춤. 화면 영향 0 |
| `api/link.js` `api/s.js` (신설) | 짧은 주소 발급·해석 | 짧은 주소가 죽는다 — 뿌린 뒤에는 주의 |
| `api/h.js` `public/h.html` (신설) | 호텔 세부 페이지 | 카드·줄이 `/h/…` 로 보내므로 **먼저 링크를 `/go` 로 되돌려야 한다** |
| `api/go.js` | 봇 제외 + 방문자·기기·나라·링크코드 | 되돌리면 가짜 클릭이 다시 섞인다 |
| `api/cron/collect.js` | 조식·정가·할인율 저장 | 되돌리면 그 칸만 빔. 가격 수집은 정상 |
| `public/shop.js` | 기록 부품·newRow·세부 링크 | 🔴 **가장 조심할 파일** — 모든 화면이 이걸 쓴다. 되돌리면 화면 전부에 영향 |
| tw-b2b `vercel.json` | `/h/:code` → 샵 (302) | 짧은 주소만 죽음. gohotelwinners 영향 0 |

### 🔴 되돌릴 수 없는 것

| 무엇 | 왜 |
|---|---|
| 행동 기록(열람·공유·찜·클릭) | 그날 있었던 일이다. 지우면 그 손님이 무엇을 했는지 영영 모른다 |
| 이미 뿌린 짧은 주소 | 남의 카톡·설명란에 있다. 우리가 회수할 수 없다 |

### 순서 규칙
1. 화면을 되돌릴 때는 **링크부터** — `/h/…` 를 `/go/…` 로 되돌린 다음 세부 페이지를 지운다. 반대로 하면 손님이 404 를 본다
2. `shop.js` 는 마지막에 되돌린다 — 모든 화면이 여기에 매달려 있다
3. 되돌린 뒤 **반드시 화면 5곳을 직접 열어 200 을 확인한다** (`/` `/japan/tokyo` `/new/japan/tokyo` `/h/411541` `/likes`)

---

## 5. 2026-09-07 · 타입 정본 (호텔 딱지 · 손님 취향 · 후기 시간축)

### 창고 — 전부 새 표다. 지워도 손님 화면이 죽지 않는다

| 표·함수 | 무엇 | 되돌리는 법 | 다른 곳 영향 |
|---|---|---|---|
| `shop_hotel_review` | 호텔별 후기 시간축 | `drop table` | **없음** — 다시 만들면 오늘부터 다시 쌓인다. 🔴 다만 **지나간 날은 되찾을 수 없다** |
| `shop_hotel_type` | 호텔 딱지 22,911곳 | `drop table` 후 `select shop_refresh_hotel_type()` 로 **4초 만에 재생성** | 없음 (아직 화면이 쓰지 않음) |
| `shop_taste` | 손님 취향 점수 | `drop table` 후 `select shop_learn_taste(60)` 로 재생성 | 없음 |
| 함수 2개 | `shop_refresh_hotel_type` · `shop_learn_taste` | `drop function` | 학습 일꾼이 400 을 돌려주고 멈출 뿐 |

🔴 **모두 «다시 만들 수 있는» 표다** — 행동 장부(`shop_view`·`shop_click`·`shop_like`)에서 매일 새로 셈하기 때문이다. **원본은 행동 장부이고, 이 세 표는 계산 결과다.** 행동 장부는 절대 지우지 않는다.

### 코드 — 커밋으로 되돌린다

| 파일 | 커밋 | 되돌리면 |
|---|---|---|
| `api/cron/collect.js` | `693245f` | 후기 저장 블록만 앞에 붙였다. 지우면 후기만 안 쌓이고 **가격 수집은 그대로** |
| `api/cron/learn.js` (신설) | `85bf76c` | 파일 삭제 시 학습만 멈춤. 화면 영향 0 |
| `vercel.json` | `10bdedc` | 크론 1줄 추가 (`20 18 * * *` = KST 03:20) |
| tw-b2b `_os/workers/registry.json` | — | 명부에서 `shop.learn` 한 줄 제거 |

### 🔴 겪은 함정 (다시 만나면 여기부터 본다)

**창고가 «WHERE 없는 UPDATE·DELETE»를 막는다.** 함수를 만들어 창구(`/rest/v1/rpc/…`)로 부르면
`21000 UPDATE requires a WHERE clause` 로 **400 이 난다.** 창고에서 직접 실행할 때는 통과하므로 놓치기 쉽다.
→ 함수 안의 모든 UPDATE·DELETE 에 `where true` 를 붙인다.
🔴 이것이 «204 를 받아도 창고를 열어 확인한다»의 실례다 — 배포는 성공했고 문법 검사도 통과했는데 **일꾼은 아무 일도 하지 않고 있었다.**

---

## 6. 2026-09-07 · 기록층(노출 장부) + 공통 순서 (SHOP_TECH §14 · 0·1단계)

### 창고

| 표·뷰·함수 | 무엇 | 되돌리는 법 | 다른 곳 영향 |
|---|---|---|---|
| `shop_impression` (신설) | 노출·자리·스킵 | `drop table` | 없음 — 기록만 멈춤. 🔴 **지나간 날은 되찾을 수 없다** |
| `shop_rank_score` (신설) | 공통 순서 | `drop table` 후 `select shop_rebuild_rank()` 로 재생성 | 🔴 **있음** — `v_shop_new_ranked` 가 이 표를 읽는다 |
| `v_shop_new_ranked` (신설) | 신상 + 순서 | `drop view` **하기 전에** `api/new.js` 의 `TBL` 을 `shop_new_live` 로 되돌려야 한다 | 🔴 순서 있음 |
| `shop_taste_config` 5줄 추가 | 순서 무게 | 그 줄만 삭제 | 순서 계산이 멈춤(함수가 null 을 읽음) |
| `shop_rebuild_rank()` (신설) | 순서 계산 | `drop function` | 일꾼이 400 을 돌려줄 뿐 |
| `shop_rebuild('feed')` (수정) | 메인 신상칸 | 아래 두 곳을 되돌린다: `order by rank_pos…` → `order by review_delta desc, review_count desc` · `from v_shop_new_ranked` → `from shop_new_live` | 메인 신상칸만 |

### 코드

| 파일 | 무엇 | 되돌리면 |
|---|---|---|
| `public/shop.js` | 카드에 `data-hid` + `bindList()` + `sessionKey()` | 노출 기록만 멈춤. 🔴 **가장 조심할 파일**(모든 화면이 쓴다) |
| `api/track.js` | `imp` 분기 | 〃 |
| `api/new.js` | 순서를 `rank_pos` 로 | 옛 순서(사실상 고정)로 돌아감 |
| `api/cron/learn.js` | 순서 계산 단계 추가 | 순서가 어제 것으로 굳음 |

### 순서 규칙
1. 되돌릴 때는 **`api/new.js` 를 먼저** 되돌리고 그다음 뷰를 지운다. 반대로 하면 신상 화면이 죽는다
2. `shop_rebuild('feed')` 도 함께 되돌린다 — 안 그러면 **메인과 도시 화면의 순서가 갈라진다**(§11 「값은 한 곳에서만」)

### 🔴 알아둘 것
- 메인 신상칸은 CDN 이 **1시간** 보관한다. 순서를 바꿔도 최대 1시간은 옛 순서가 보인다(정상)
- 노출 장부는 **손님이 목록에서 무언가를 누를 때만** 보낸다. 클릭이 없으면 0줄이 정상이다

## 7. 2026-09-07 · 감시 · 실험 장치 (2·3단계)

| 표·함수·뷰 | 되돌리는 법 | 다른 곳 영향 |
|---|---|---|
| `shop_health` · `shop_health_check()` | `drop table` · `drop function` | **없음** — 점검만 멈춘다. 🔴 다만 조용한 실패를 다시 사람이 찾아야 한다 |
| `shop_experiment` · `shop_arm()` · `v_shop_exp_daily` | `drop` | **없음** — 아직 아무 화면도 `shop_arm` 을 부르지 않는다 |
| `api/cron/health.js` (신설) | 파일 삭제 | 없음 |
| `vercel.json` 크론 1줄 | 그 줄 삭제 | 없음 |

🔴 **실험을 끄는 가장 안전한 방법은 파일을 지우는 것이 아니라 `update shop_experiment set status='off'` 한 줄이다.** 그러면 모든 손님이 A(옛 방식)로 간다.

## 8. 2026-09-07 · 오픈 준비 (처리방침 · robots · sitemap)

| 파일 | 무엇 | 되돌리면 |
|---|---|---|
| `public/privacy.html` | 2조·3조에 「목록 표시·선택 기록」 추가 | 🔴 **되돌리지 말 것** — 담고 있는데 안 적혀 있으면 그 자체가 위반이다 |
| `api/robots.js` (신설) | 개발용 주소는 색인 차단·진짜 도메인만 개방 | 파일 삭제 시 404 → 구글이 «제한 없음»으로 본다 |
| `api/sitemap.js` (신설) | 실제로 있는 주소만 | 파일 삭제 시 404 |
| `vercel.json` | 주소 2줄 추가 | 그 줄 삭제 |

🔴 **도메인을 옮긴 뒤에는 `robots.js` 를 되돌리지 않는다** — 개발용 주소가 색인되면 진짜 도메인의 순위를 깎는다. 판단은 주소를 보고 자동으로 한다(고칠 것 없음).
🔴 **아직 남은 것**: 없는 주소(`/about` 등)가 목록 화면으로 떨어져 **200 을 준다**. `/:country` 규칙이 무엇이든 받기 때문이다. 색인 문제는 sitemap 으로 줄였지만 근본 해결은 「없는 나라·도시면 없는 페이지를 그린다」이다.

## 9. 2026-09-07 · 쇼츠 자동 연결 · 관리자 탭

| 것 | 되돌리는 법 | 다른 곳 영향 |
|---|---|---|
| tw-b2b `api/cron/push-to-shop.js` (신설) | 파일 삭제 + `vercel.json` 크론 1줄 | 자동 등록만 멈춤. 스튜디오 영향 **0** |
| `shop_publish_log` (신설) | `drop table` | 없음 — 수신 기록만 멈춤 |
| `v_shop_video_admin` (신설) | `drop view` **전에** 관리자 창구 `tab=shorts` 를 먼저 되돌린다 | 🔴 순서 있음 |
| `api/hook/publish.js` | 커밋 되돌리기 | 🔴 **되돌리면 사진이 다시 옛 규격(1200×900)으로 받힌다** — 규격 부분은 되돌리지 말 것 |
| `api/admin/index.js` · `public/admin.html` | 커밋 되돌리기 | 탭 2개만 사라짐 |

🔴 **자동 연결을 멈추는 가장 안전한 방법은 파일 삭제가 아니라 tw-b2b `vercel.json` 의 크론 1줄을 지우는 것이다.** 창구는 그대로 두면 손으로 부를 수 있다.

### 🔴 2026-09-07 · 시험 자료를 심었으면 «그 영향을 받은 줄»까지 되돌린다

감시가 실제로 고장을 잡는지 보려고 `shop_health` 의 **어제 행**에 오늘과 같은 서명을 심었다.
확인 뒤 «어제 행»은 지웠지만, **그 시험 때문에 ok=false 로 저장된 «오늘 행»을 지우지 않았다.**
그 결과 대표님 화면에 **가짜 빨간불 1건**이 떴고, 대표님이 먼저 발견하셨다.

🔴 **규칙** — 시험 자료를 심었으면 **심은 줄만이 아니라 그것 때문에 바뀐 줄까지** 되돌린다.
판정을 남기는 표(`shop_health` 처럼)는 시험 뒤 **반드시 한 번 더 정상 실행**해서 판정을 새로 쓴다.
확인: 재실행 후 9가지 전부 정상 · `bad: []`.

## 10. 🔴 2026-09-07 사고 — 열쇠를 «추가»했더니 손님 화면 두 곳이 죽었다

**증상** — 세부 화면 `/h/…` 와 신상 화면 `/new/…` 가 열리면 **첫 화면으로 튕겼다.** 대표님이 주소를 붙여넣으셨는데 주소창이 홈으로 바뀌어 발견.

**원인**
```
api/h.js · api/new.js · api/s.js
  const KEY = process.env.SUPABASE_ANON_KEY || process.env.SUPABASE_SERVICE_ROLE_KEY;
                          ↑ 앞의 것을 먼저 쓴다
```
🔴 **그 열쇠가 «없던 동안»에는 뒤의 SERVICE_ROLE 로 넘어가 잘 돌았다.**
오늘 회원 가입을 열려고 `SUPABASE_ANON_KEY` 를 등록하자 **앞의 것이 잡히면서** 창고가 `401 permission denied` 를 돌려주기 시작했다 — `anon` 역할에는 이 뷰들의 읽기 권한이 없다(실측: `v_shop_detail`·`v_shop_card`·`shop_hotel` 모두 anon=false).
화면 코드는 자료를 못 받으면 `location.href = '/'` 로 홈에 보내게 되어 있어 **오류가 아니라 「튕김」으로 보였다.**

**고침** — 서버에서만 도는 창구이므로 `SERVICE_ROLE` 만 쓰게 했다(3개 파일). 복구 확인: 세 창구 정상 · 화면 5곳 200.

🔴 **배운 것 세 가지**
1. **열쇠를 «하나 추가»하는 것이 «다른 화면»을 죽일 수 있다.** `A || B` 우선순위 코드는 지뢰다 — B로 잘 돌던 것이 A가 생기는 순간 바뀐다
2. **환경변수를 추가·변경한 뒤에는 그 이름을 쓰는 파일을 전부 찾아 확인한다**(`grep`). 오늘은 auth.js 만 보고 끝냈다
3. **화면이 「오류」가 아니라 「튕김」으로 실패하면 아무도 눈치채지 못한다.** 자가 점검의 「손님 화면 5곳」은 200 만 보므로 이 사고를 **잡지 못했다** — 화면이 실제로 200 을 주고 있었기 때문이다

## 11. 2026-09-07 · 상품 카드 줄이 뒤죽박죽이던 것

**증상** (대표님 화면) — 「교토 여행, 추천 상품」 줄에서 **첫 카드만 크고**, 이름이 한 줄인 카드와 두 줄인 카드의 **값 자리가 서로 어긋났다.**

**원인** — 담기 단추(+)를 붙이면서 카드를 `.pwrap` 으로 **한 겹 쌌는데**, 너비 규격 `flex:0 0 46%` 는 **안쪽 `.pcard` 에만** 남아 있었다.
줄(`.prail`)이 실제로 늘어놓는 것은 `.pwrap` 이므로 **규격이 먹지 않았고**, 카드마다 너비·그림 크기가 달라졌다.
🔴 **한 겹 감쌀 때 「줄이 보는 쪽」으로 규격을 옮기지 않으면 이렇게 된다.**

**고침 (`public/shop.css` 한 곳)**
| | |
|---|---|
| `.pwrap` | 너비 `46%`·스냅을 **바깥으로** 옮김 |
| `.pcard` | 감싸개를 꽉 채우게 (`width/height:100%`) |
| `.prail` | `align-items:stretch` — 칸 높이를 서로 맞춤 |
| `.ptitle` | `max-height` → **`height:38px` 고정** — 이름이 한 줄이어도 두 줄 자리를 차지해 값 자리가 어긋나지 않는다 |

**되돌리기** — 커밋 하나. 규격은 `shop.css` 한 장이라 **상품이 나오는 모든 화면**(메인·도시·세부·상품 목록)이 함께 돌아간다.

## 12. 2026-09-08 · 상품 카드에 별점 한 줄을 넣음 (§37 「안 1」)

**바꾼 것 세 곳**
| 곳 | 무엇을 |
|---|---|
| 창고 뷰 `v_shop_product` | 칸 두 개(`rating`·`review_count`)를 **뒤에 덧붙임**. 앞의 15칸은 순서·이름 그대로다 |
| `public/shop.js` | `rateLine(p)` 신설 + `productCard` 에서 이름 아래·값 위에 한 줄 |
| `public/shop.css` | `.prate`·`.pstar`·`.prcnt` 규격 (새 색은 만들지 않았다 — `--t1`/`--t2` 안에서) |

🔴 **창구 `api/products.js` 는 고치지 않았다.** 이미 `select=*` 라 뷰에 칸이 생기는 순간 함께 나간다.
파일을 건드리지 않는 쪽이 되돌릴 것도 하나 줄어든다.

**되돌리기**
```
① 화면만 되돌린다  shop.js·shop.css 커밋 2개를 되돌리면 줄이 사라진다 (창고는 그대로 둬도 해가 없다)
② 창고까지 되돌린다  create or replace view v_shop_product as
                     select id,source,ext_id,title,category,country_slug,city_slug,image_url,
                            link_url,price,currency,badge,sort_order,active,synced_at
                       from shop_product
                      where active and synced_at > (now() - '45 days'::interval);
```
🔴 **되돌릴 수 없는 것은 없다.** 자료를 지우거나 다시 받는 일이 아니라, **이미 창고에 있던 칸을 내보내기만** 한 것이다.
사진처럼 「두 번 오가는 요금」이 붙는 종류가 아니다.

**확인한 것 (실측 2026-09-08)**
- 뷰 1,097줄 중 평점 있는 것 **516** · 권한(`service_role` SELECT) 그대로
- 배포된 `shop.js` 를 그대로 실행해 도쿄 상품 20개에 먹여 보니 **17개에 줄이 그려지고 3개는 줄 자체가 없다** — 평점 있는 자료 수 17과 정확히 같다
- 평점은 있는데 후기 수가 없는 상품은 **0개**(창고 실측). 그래도 그때는 `★ 4.8` 만 나오게 해 두었다

## 13. 2026-09-08 · 담기 단추를 하트로 · 사진 안 할인 딱지 자리

**왜** — 대표님: *«+버튼을 하니깐 이상해. 클릭하니깐 검정색이라서 이상해. 그냥 하트로 하자. 할인율은 사진 오른쪽 안에 넣자»*
켜졌을 때 동그라미 **배경**이 검정(`--t1`)으로 차고 `+` 가 45도 돌아 **×(닫기)** 로 보였다. 「담겼다」가 아니라 「지운다」로 읽힌다.

| 곳 | 무엇을 |
|---|---|
| `shop.js` `cartBtn` | `+` path → **호텔 찜과 같은 하트 path**. `aria-label` 「담기」→「찜하기」 |
| `shop.css` `.cartb` | `.cartb.on{background:var(--t1)}` **삭제** · `.cartb.on svg{stroke/fill:#D64545}` — 호텔 찜(`.ic.on`)과 같은 빨강 |
| `shop.js` `offBadge` 신설 | `discount_pct` 가 있을 때만 `<span class="poff">15%</span>`. 사진(`.pimg`) 안에 넣는다 |
| `shop.css` `.poff` | 오른쪽 **아래**(`right:8px;bottom:8px`) — 오른쪽 위는 하트, 왼쪽 위는 갈래 딱지가 쓴다 |

🔴 **저장되는 곳은 안 바꿨다.** 상품은 `tw_cart`, 호텔 찜은 `tw_likes` 로 여전히 따로다. **모양만 같아졌다.**
⚠️ `likes.html` 의 「내가 담은 상품」 문구는 **그대로 뒀다** — 대표님이 정하신 문구라 여쭙고 바꾼다.

**되돌리기** — `shop.js`·`shop.css` 커밋 2개. 창고는 건드리지 않았다.

**확인** — 배포된 `shop.js` 를 그대로 실행: 할인율 있으면 `poff` 그려짐 · 없으면 안 그려짐 · 옛 `+` path 사라짐 · 별점 줄 유지. `shop.css` 에 `cartb.on{background` **0건**.

## 14. 2026-09-08 · 할인 표시를 「값 옆 취소선」으로 (대표님 이미지 지시)

**왜** — 대표님이 `20,128원 부터  ~~22,393원~~` 이미지를 주시며 *«할인부분은 이미지처럼 해야 되는거 아니야»*.
%딱지만으로는 **얼마나 싼지 금액으로 보이지 않는다.**

| 곳 | 무엇을 |
|---|---|
| `shop.js` `listPrice` 신설 | `list_price > price` **일 때만** 값 옆에 취소선 정가 |
| `shop.css` `.plist` | 12px · `--t3` · `line-through` · 카드가 좁으면 아랫줄로 내려가게 (`nowrap` 은 숫자 안쪽에만) |

🔴 **`list_price === price` 면 안 그린다.** 「22,393원 부터 22,393원」은 손님이 속았다고 느낀다.
🔴 사진 안 %딱지(`offBadge`)는 **그대로 둔다.** 같은 자료(`list_price`·`discount_pct`)에서 나오므로 **더 받을 것이 없다.**
멀리서 훑을 때는 딱지가, 살까 말까 할 때는 취소선 숫자가 쓰인다.

**되돌리기** — 커밋 2개. %딱지만 끄려면 `shop.css` 에서 `.poff{display:none}` 한 줄이면 된다(코드는 그대로 둬도 된다).

**확인** — 배포본 실행: 정가>값 → `20,128원 부터 22,393원` · 정가=값 → 취소선 없음 · 정가 없음 → 취소선 없음.

## 15. 2026-09-09 · KKday 2차 수집 준비 (장부 칸 추가)

**바꾼 것** — 창고 `shop_kk_sync` 에 칸 셋을 **덧붙였다**: `deal_on`(date) · `deal_ok`(bool) · `deal_note`(text).
1차 진행(`synced_on`·`ok`)과 **섞이지 않게 따로** 둔 것이다. 기존 칸·자료는 하나도 건드리지 않았다.

**함께 한 것** — 1차에서 「상품 화면 자체가 없다」로 판정된 **33개**(공항픽업 18 · 호텔 12 · 크루즈 2 · 삭제 1)를
`deal_ok=false` 로 **미리 적어 두었다.** 2차에서 다시 열어 헛되이 시간을 쓰지 않게 하기 위함이다.
→ 2차 대상은 **1,064개**.

**되돌리기**
```sql
alter table shop_kk_sync drop column if exists deal_on,
  drop column if exists deal_ok, drop column if exists deal_note;
```
🔴 **되돌릴 수 없는 것은 없다.** 자료를 지우거나 다시 받는 일이 아니라 **빈 칸을 셋 만든 것**뿐이다.
2차 도중에 되돌리면 「어디까지 했나」만 잃는다(상품 자료는 그대로).

**아직 모르는 것** — 할인·조건이 **글(HTML)만 읽어도 나오는지**는 확인하지 못했다.
서버에서는 KKday 가 403 으로 막아 여기서 잴 수 없다. **크롬 안에서 3개로 먼저 재는 절차**를 명령서 §1 에 넣었다.
되면 40분, 안 되면 3~4시간이다 — **짐작으로 긴 쪽을 고르지 않는다.**

## 16. 2026-09-09 · 예약 수 딱지 (사진 안 왼쪽 아래)

**왜** — Cowork 2차 수집으로 `booked_text` 가 **371개** 들어왔는데 손님 화면에 자리가 없었다.

| 곳 | 무엇을 |
|---|---|
| `shop.js` `bookedBadge` 신설 | 사진 안 왼쪽 아래. **「10K+」를 「10,000+」로 풀어 쓴다**(한국 손님에게 K 는 낯설다) |
| `shop.css` `.pbook` | 반투명 검정 위 흰 글씨 — 갈래 딱지(`.pcat`, 흰 바탕)와 **다른 옷**을 입혔다 |

🔴 **별점 줄에 붙이지 않았다.** 예약 수 371개 중 **370개가 평점도 있다** — 같은 줄에 두면
휴대폰 카드(175px)에서 「★ 4.8 (23,114) · 10,000+ 예약」이 되어 두 줄로 터진다.

**사진 안 네 자리가 이걸로 다 찼다** — 왼위 갈래 · 오른위 하트 · 왼아래 예약 수 · 오른아래 할인율.
🔴 **다음에 무엇을 더 넣자는 말이 나오면 「자리가 없다」가 답이다.** 사진 위에 다섯 번째를 얹지 않는다.

**되돌리기** — 커밋 2개. 딱지만 끄려면 `.pbook{display:none}` 한 줄.

**확인 (실측)** — 배포본 + 마카오 실제 자료 20개: 별점 12 / 예약 10 / 할인 13 / 취소선 13 이 그려졌고
**창고 자료 수와 네 가지 모두 정확히 일치.** 글자 변환도 통과(10K+→10,000+ · 1.5K+→1,500+ · 400+→400+).

## 17. 2026-09-09 · KKday 값 매일 받기 구조 (문서만 · 코드 변화 없음)

**바꾼 것** — **없다.** `KKDAY_DAILY.md` 명령서를 만들고 `boot.md §4` 에 등재했을 뿐이다.
창고 표도, 화면도, 창구도 그대로다. **되돌릴 것이 없다.**

**재보다가 만들었다가 «끈» 것 두 개**
| 무엇 | 지금 상태 |
|---|---|
| `travelwinners-shop` `api/kkprobe.js` | **410 만 돌려준다.** Vercel 이 KKday 를 부를 수 있나 재본 시체 |
| `.github/workflows/kk-probe.yml` | **자동 실행 끔**(`workflow_dispatch` 만). Actions 도 403 이었다 |
| `_probe/kk-actions-result.json` | 🟢 **증거로 남겨 둔다.** 지우지 마라 — 「Actions 로 해보자」가 또 나오면 이걸 보여준다 |

🔴 **되살리지 마라.** 되살려도 403 이다. 헤더가 아니라 **IP** 가 막힌 것이다.

## 18. 2026-09-09 · 메인 이벤트 칸을 «세 갈래»로

**왜** — 이벤트가 0개인 채로 시간이 흘러 「곧 새 이벤트가 열립니다」만 계속 보였다.
대표님: *«이벤트 진행중이면 보여지고 아직 이벤트 없을때는 안보이게 할수 있나»* → *«지난이벤트를 보게 하자»*

🔴 **이것은 2026-09-07 결정(「0개여도 칸은 남긴다」)을 대표님이 «직접» 뒤집으신 것이다.**
그때 걱정하신 것(칸이 사라지면 손님이 이벤트 존재를 모른다)은 **맨 아래 한 줄로 지켰다.**

| 상황 | 손님 화면 |
|---|---|
| 진행 중 있음 | 「진행 중인 이벤트」 — 전과 같다 |
| 진행 중 없고 **지난 것 있음** | **「지난 이벤트 · 당첨자 발표」**로 바뀐다 |
| 둘 다 없음 (지금) | **칸을 안 그린다** + 맨 아래 「이벤트 안내」 한 줄 |

🔴 **지난 이벤트 목록을 지우지 않는 진짜 이유는 둘이다.**
① **의무** — 응모 화면에서 「당첨 안내」를 목적으로 이메일을 받는다. 끝난 뒤에도 발표를 볼 수 있어야 한다.
② 🔴 **B2B 실적 자료** (2026-09-09 대표님 지적) — 호텔이 숙박권을 내놓기 전 가장 궁금해하는
「이 회사 진짜 하나?」의 답이 이 목록이다. **손님보다 호텔에게 더 중요할 수 있다.**

| 곳 | 무엇을 |
|---|---|
| `api/main.js` | `evDone` 조회 추가 → `payload.events = { open, past }`. **못 읽어도 화면은 그린다**(try) |
| `public/index.html` | 세 갈래 + 맨 아래 한 줄. `d.events` 가 없으면 빈 배열로 본다(옛 창구 대비) |

**되돌리기** — 커밋 2개. 창고는 건드리지 않았다.
🔴 **되돌린 뒤에는 `delete from shop_feed where key='main'` 를 꼭 한다** — 아래 함정 참고.

**🔴 걸린 함정 둘 (같은 실수 반복 금지)**
1. **미리 구운 것이 먼저 나간다.** `api/main.js` 를 고쳐도 `shop_feed.main` 에 옛 덩어리가 있으면
   손님은 계속 옛 것을 받는다. 30분마다 도는 일꾼이 갈아주지만, **바로 보려면 그 줄을 지운다.**
2. **커밋 전에 파일이 되돌아갔다.** 검사 준비로 `cp ../repo/api/*.js api/` 를 돌리는 바람에
   **수정본이 원본으로 덮여** 그대로 올라갔다. 배포해도 아무 변화가 없어 5분을 헤맸다.
   → 🔴 **커밋 직전에 「고친 내용이 파일에 있는지」 한 번 확인한다**(`grep evDone`).

**확인 (실측)** — 창구 `events:{open:0,past:[]}` 나감 · 미리 구운 것에도 들어감 ·
손님 화면에 세 갈래가 다 들어 있고 **지금은 칸 없이 「이벤트 안내」 한 줄만** 그려진다.

## 19. 2026-09-09 · 로그인하면 «다른 기기 찜»도 따라오게

**왜** — 대표님: *«찜 버튼도 다른 서비스는 로그인해야 할 수 있게 되어 있는데 이 부분 체크해줘»*

**체크 결과 — 우리 구조가 오히려 낫다.** 다른 서비스가 로그인을 요구하는 이유는 «저장할 곳이 계정뿐»이라서다.
우리는 **로그인 없이도 창고(`shop_like`·`shop_cart`)에 방문자 번호로 저장**하고 있었다.
`likes.html` 에 이미 적혀 있다 — *«로그인을 먼저 요구하면 대부분 떠난다»*(와그·마이리얼트립 조사 후 결정).
🔴 **그 판단은 그대로 둔다. 로그인을 강제하지 않는다.**

**진짜 구멍은 「읽는 쪽」이었다** — `api/me.js` 가 **오직 방문자 번호로만** 읽었다.
`auth.js` 의 `linkVisitor` 가 로그인 때 `member_id` 를 찍어 주고 있었는데도, **읽을 때 그 번호를 안 썼다.**
→ 휴대폰에서 찜하고 노트북에서 로그인하면 **아무것도 안 보인다.** 로그인의 유일한 이유가 작동하지 않았다.

| 곳 | 무엇을 |
|---|---|
| `api/me.js` | `memberIdOf(req)` 신설 — 열쇠를 창고에 물어 확인 후 회원 번호를 얻는다. 찜·담기를 `or=(visitor, member_id)` 로 읽는다 |
| `api/auth.js` | `linkVisitor` 에 **`member_id=is.null`** 추가 |
| `public/likes.html` | `/api/me` 호출에 `Authorization: Bearer <열쇠>` 를 붙인다 |

🔴 **회원 번호를 손님에게서 받지 않는다.** `?mid=3` 처럼 숫자만 바꿔 보내면 **남의 찜이 그대로 보인다.**
반드시 열쇠를 창고에 물어 확인한 뒤, **창고가 알려준 번호만** 쓴다. 열쇠가 가짜·만료면 «로그인 전»으로 다룬다.

🔴 **`member_id=is.null` 이 없으면 «남의 찜을 빼앗는다».** 한 컴퓨터를 둘이 쓰는 경우가 실제로 있다.
A 가 로그인해 찜을 모아둔 브라우저에서 B 가 로그인하면, 조건이 없을 때 **A 의 찜이 통째로 B 것이 된다.**
되돌릴 방법도 없다. **이 조건을 빼지 마라.**

**「최근 본 호텔」·「내가 공유한 링크」는 회원으로 넓히지 않았다** — 기기마다 다른 것이 자연스럽다.

**되돌리기** — 커밋 3개. 창고는 건드리지 않았다(칸은 원래 있었다).

**확인 (실측)** — 로그인 전 · 가짜 열쇠 · 열쇠 없음 세 경우 모두 오류 없이 «로그인 전»처럼 처리됨 ·
실제 찜이 있는 방문자로 조회하니 찜 1 · 담기 1 · 최근 1 이 그대로 나옴(기존 손님 화면 그대로).

## 20. 2026-09-09 · 관리자 「값 변동 · 상품」이 빈 화면이던 것

**왜** — 대표님: *«kkday 상품페이지가 정상적으로 안 나와»*

**고장이 아니었다.** 이 화면은 `v_shop_product_move` 로 **「어제 값과 오늘 값의 차이」**를 보여주는데,
`shop_product_price` 에 **2026-09-08 하루치(1,095줄)뿐**이라 비교할 짝이 없어 표가 통째로 비었다.
🔴 **내일 새벽 예약 작업이 한 번 더 돌면 저절로 채워진다.** 뷰(`lag()` 로 앞 줄과 비교)는 정상이다.

**그래도 빈 화면은 「고장 난 것」으로 보인다** — 그동안 볼 것을 준다.

| 곳 | 무엇을 |
|---|---|
| `api/admin/index.js` | `product_now` 추가 — `v_shop_product` 후기 많은 순 100개(값·정가·할인·평점·예약 수) |
| `public/admin.html` | 변동이 없으면 **「오늘 담긴 것」 표**를 대신 그린다. 상품이 아예 없을 때만 빈 안내 |

🔴 **안내 문구도 갱신했다.** 옛 문구는 *«대표님이 말씀하시면 클로드가 크롬으로 받아옵니다»* 였다 —
이제 **매일 새벽 1시 10분에 예약 작업이 저절로 받는다.** 「대표님 컴퓨터가 켜져 있어야 한다」를 함께 적었다.

**되돌리기** — 커밋 2개. 창고는 건드리지 않았다.

**확인 (실측)** — 현황표에 나올 100개: 값 100 · 평점 100 · 예약 100 · 할인 37. 배포 확인 완료.

## 21. 2026-09-09 · 상품 값 변동을 «호텔과 같은 수준»으로

**왜** — 대표님: *«이것도 호텔처럼 카테고리 나라별 도시별로 있어야 되고 일자별로 어떻게 변동되는지 형태가 있어야 될 것 같애»*

| 곳 | 무엇을 |
|---|---|
| 창고 뷰 `v_shop_product_move` | `country_slug` 를 **뒤에 덧붙임**. 없어서 나라로 거를 수 없었다 |
| `api/admin/index.js` | 상품 거르개(`pcat`·`pcountry`·`pcity`·`pq`) + 상품 상세에 이름·갈래·도시·링크 |
| `public/admin.html` | 갈래/나라/도시/이름 고르개 + 각 줄에 **「일자별」 단추** → 잰 날마다 값·앞과 차이·막대 |

🔴 **상품 거르개는 호텔 것(`PM_LIST`)과 «따로»(`PM_PROD`) 둔다.** 한 통에 담으면
탭을 오갈 때 서로의 조건이 남아 결과가 0 이 된다. 성급은 상품에 없다.
🔴 **갈래 목록은 «거르개가 걸리기 전» 전체에서 뽑는다.** 걸린 목록에서 뽑으면
「교통」을 고른 순간 다른 갈래가 사라져 **되돌아갈 수 없다.**
🔴 상품은 숙박일이 없다 — 호텔의 「잰 날 × 숙박일」 표 대신 **한 줄짜리 일자별**이면 충분하다.

**되돌리기** — 커밋 2개. 뷰는 `country_slug` 만 빼면 원래대로:
```sql
-- v_shop_product_move 에서 맨 뒤 country_slug 한 칸만 빼고 CREATE OR REPLACE
```

**🔴 걸린 함정 — `Promise.all` 순서**
`prodCats` 를 배열 «중간»에 넣고 받는 쪽은 «맨 뒤»에 적어, **갈래 목록 자리에 상품 총수가 들어갈 뻔했다.**
→ 배열에 하나를 끼워 넣으면 **받는 순서를 눈으로 다시 센다.**

**🔴 검사가 잡아준 것** — 템플릿 닫는 짝(`` ` `` + `}`)이 하나 부족해 화면이 통째로 안 그려질 뻔했다.
`npm run check` 가 「Missing } in template expression」으로 잡았다. **올리기 전 검사를 건너뛰지 마라.**

**확인 (실측)** — 거르개: 전체 1,097 → 교통 237 → 일본 교통 59 → 도쿄 교통 5 로 제대로 좁혀짐.
일자별은 아직 **1일치**라 「앞과 차이」가 빈칸이다 — 내일 새벽부터 채워진다.

## 22. 2026-09-09 · 🔴 자가 점검이 «등록되지 않아» 한 번도 돌지 않던 것

**왜** — 대표님: *«여기는 아무것도 없어»*

**만들어만 놓고 아무도 부르지 않았다.** `api/cron/health.js` 는 2026-09-07 에 만들었는데
**`vercel.json` 의 `crons` 에 넣지 않았다.** 9/7·9/8 기록은 사람이 손으로 부른 것이다.
🔴 **`api/cron/learn.js` 도 똑같았다.** 두 개 다 「만들고 등록을 잊은」 같은 실수다.

| 곳 | 무엇을 |
|---|---|
| `vercel.json` | `/api/cron/health` **하루 두 번**(UTC 5,17 = 한국 14:00 · 02:00) · `/api/cron/learn` **하루 한 번**(UTC 16:30 = 한국 01:30) |
| `api/cron/health.js` | **한국 날짜**로 적는다 + **「KKday 값이 오늘 것인가」** 점검 항목 신설 |
| `api/admin/index.js` | 한국 날짜로 보고, 오늘 것이 없으면 **가장 최근 점검**을 준다 |
| `public/admin.html` | 빈 화면 대신 「마지막 점검 · 날짜」를 보여준다 |

🔴 **시간 순서를 맞췄다** — 한국 01:10 KKday 값 받기 → 01:30 학습(상품 값 스냅샷) → 02:00 자가 점검.
**앞의 것이 끝난 뒤에 뒤의 것이 본다.** 순서가 뒤집히면 점검이 늘 「어제 것」을 본다.

🔴 **새 점검 항목이 예약 작업을 감시한다.** 노트북이 꺼져 KKday 를 못 받으면
「🔴 N일째 받지 못했다」가 관리자 화면에 뜬다. **조용히 지나가지 않는다.**

**켜자마자 실제 이상 3건을 잡았다** — 「순서가 어제 것으로 굳었다」·「순서 서명이 어제와 같다」·「호텔 딱지 0곳」.
전부 **학습 일꾼이 안 돌아서**였다. 등록 후 한 번 돌리니 **이상 0건**이 됐다(호텔 23,050 · 순서 3,705 · 닮은 곳 76,835).

**🔴 알아낸 것 — 학습 일꾼이 상품 값 스냅샷(`prod_snap`)도 찍는다.**
그런데 그것은 **창고에 있는 값을 그대로 오늘 날짜로 복사**하는 것이다 — KKday 에서 다시 받는 것이 아니다.
그래서 9/9 스냅샷 1,097줄이 생겼지만 **오름 0 · 내림 0** 이다.
**진짜 값 변동은 예약 작업이 KKday 에서 새 값을 받아온 다음 날부터 보인다.**

**되돌리기** — `vercel.json` 에서 두 줄을 빼면 예전처럼 안 돈다(=예전 상태). 나머지는 커밋 3개.

## 23. 2026-09-09 · 떠 있던 「이벤트 안내」 한 줄을 발판으로 옮김

**왜** — 대표님: *«밑에 글자로 이런식으로 있으니깐 전체 디자인 흐름에 안 맞는거 같아. 이상해»*

§18 에서 이벤트 칸을 숨기면서 «맨 아래 조용한 한 줄»을 남겼는데,
그 줄이 **어느 칸에도 속하지 않고 혼자 떠 있었다.** 화면의 나머지는 모두 칸(`.sec`) 안에 들어 있다.

**바로 아래 발판에 «같은 모양의 링크»가 이미 모여 있었다** — 「이용약관 · 개인정보 처리방침 · 문의」.
거기에 「이벤트」를 맨 앞에 넣었다. **새 자리를 만들지 않고 있는 자리에 넣은 것이다.**

| 곳 | 무엇을 |
|---|---|
| `public/index.html` | 떠 있던 한 줄 **삭제** · 발판 링크 줄 맨 앞에 「이벤트」 |
| `public/shop.js` | 다른 화면들의 발판에도 **같이** 넣었다 |

🔴 **발판을 두 곳에서 다르게 두지 않는다.** `index.html` 과 `shop.js` 에 발판이 «따로» 적혀 있어
한쪽만 고치면 **첫 화면과 나머지 화면의 발판이 달라진다.** 둘 다 고쳤다.
🔴 **조건을 걸지 않았다.** 약관처럼 «늘 있는 문»이다 — 이벤트가 있을 때는 위에 큰 칸이
따로 나오므로 겹쳐 보이지 않는다. 조건이 없어져 코드도 단순해졌다.

**§18 의 세 갈래는 그대로다** — 이벤트가 생기면 큰 칸이 **원래 자리**(신상 아래)로 돌아온다.

**되돌리기** — 커밋 2개.

**확인 (실측)** — 손님 화면에 떠 있던 줄 **0건** · 발판 링크 **1건** · 다른 화면 발판 **1건** ·
세 갈래 코드는 그대로 남아 있음.

## 24. 2026-09-09 · 호텔 이름·동네 채우기 (①②③ 전부)

**대표님 지시** — *«순서대로 다 진행해»*

### ① 한국어 이름 — 🔴 «고칠 것이 없었다». 앞선 내 판단이 틀렸다

처음에 「명부에 한국어가 있는 22곳을 채울 수 있다」고 했는데 **틀렸다.**
그 22곳은 `hotel_name` 칸 자체에 한글이 들어 있어 **이미 화면에 한국어로 나오고 있었다.**
다시 세니 **화면에 한국어가 아예 없는 곳은 27곳**이고, **그 27곳은 명부에도 한국어가 없다.**
→ **아고다가 진짜로 주지 않는다.** 대표님 추측이 맞았다. **고칠 수 없다.**

### ② 동네 — 창고 뷰 두 개를 고쳤다 (화면 5곳은 안 건드렸다)

`location_note` 를 읽는 곳이 화면 5곳인데, **뷰 두 개(`v_shop_card`·`v_shop_city_hotel`)만 고치니 전부 따라왔다.**

```sql
COALESCE(NULLIF(h.location_note,''),
  CASE WHEN ht_area.area_name ~ '[가-힣]' AND ht_area.area_name !~ '[A-Za-z]'
       THEN ht_area.area_name ELSE NULL END)
```
🔴 **창고 원본(`shop_hotel.location_note`)은 덮지 않았다** — 아고다가 준 값이 더 정확할 수 있다.

**🔴 여기서 두 번 걸렸다 (같은 실수 반복 금지)**
1. **처음엔 조건 없이 `area_name` 을 그대로 썼다** → 손님 화면에 「Minami구」·「District 1」이 뿌려졌다.
   학습표 `area_name` 17,205개 중 **16,480개가 영어**다. **바로 되돌렸다.**
2. **그래서 「한글이 있으면」으로 걸었더니 「Minami구」가 그대로 통과했다** — **'구' 가 한글이기 때문이다.**
   → 🔴 **「한글이 있으면」이 아니라 «영어 글자가 하나도 없으면»으로 걸어야 한다.**

**영어 표기를 한국어로 바꾼 곳** — 창고 16곳 + 학습표 18곳 (`Minami구`→`미나미구` 등 11종).
🔴 **두 곳 다 고쳐야 한다.** 창고만 고치면 학습이 대신 쓰이는 자리는 그대로 영어다(실제로 그랬다).

**결과** — 카드 1,306장 중 동네 **699 → 756곳** · **영어 섞임 0곳**.

### ③ 자가 점검에 항목 둘 추가

| 항목 | 무엇을 본다 | 언제 알린다 |
|---|---|---|
| **호텔 한국어 이름** | 한국어가 아예 없는 곳 수 | **40곳을 넘으면** (지금 24곳) |
| **호텔 동네 표기** | 빈 곳 수 · **영어 섞인 곳 수** | 🔴 **영어가 하나라도 섞이면** |

🔴 **성급은 세지 않는다.** 명부에도 성급이 있는 곳이 0곳이다 — 아고다가 등급을 안 매긴 숙소이고,
다시 받아도 안 채워진다. **「빠진 것」으로 세면 영영 빨간불이 꺼지지 않는다.**

🔴 **점검이 처음엔 「확인 못 함」을 냈다** — `v_shop_card` 는 값 계산이 붙어 무겁다.
같은 자리를 쓰는 **가벼운 `v_shop_city_hotel`** 로 바꾸니 통과했다.

**되돌리기** — 뷰 둘은 `COALESCE` 를 `h.location_note` 로 되돌리면 된다.
이름 바꾼 34곳은 되돌릴 필요가 없다(영어 표기가 더 나을 이유가 없다).

## 25. 2026-09-09 · 「어디 가세요?」 안에서 고르기와 찾기를 함께

**왜** — 대표님: *«호텔명을 검색할 수 있게 검색도 같이 넣으면 어때? 하나에서 선택과 검색 같이 할 수 있는 방법이 없을까?»*

**찾는 길이 셋이었고 서로 몰랐다.**
| 어디 | 무엇을 했나 |
|---|---|
| 위 「어디 가세요?」 시트 | **도시만** |
| 맨 아래 「찾는 호텔이 없나요」 | 문장 검색 — **거기까지 내려온 손님만** 썼다 |
| 창구 `api/search.js` | 🟢 **이미 호텔 이름으로도 찾을 줄 알았다** |

🔴 **창구는 다 할 줄 알았고 화면만 길을 안 열어 준 것이었다.** 실측: 「힐튼」 43곳 · 「메리어트」 36곳 · 「코코」 27곳.

| 곳 | 무엇을 |
|---|---|
| `shop.js` `openSheet` | 시트 맨 위에 **입력칸** · `paintSheet`·`sheetType`·`sheetGo` 신설 |
| `index.html` | 맨 아래 검색칸 **삭제** → 「위 「어디 가세요?」에 호텔 이름도 넣어보세요」 안내 + 시트 여는 단추 |

**시트가 상황마다 다르게 그린다**
| 손님이 친 것 | 나오는 것 |
|---|---|
| 없음 | 🔴 **지금까지와 똑같이 도시 전부** — 기본을 바꾸지 않는다 |
| 도시 이름 | 걸린 도시 (**즉시** · 창구를 부르지 않는다) |
| 호텔 이름 | 도시 + **호텔 6개 + 「모두 보기」** |
| 못 찾음 | 「그래도 찾아보기 ›」 |

🔴 **타자마다 창구를 부르지 않는다** — 350ms 쉬었을 때만 한 번.
🔴 **늦게 온 옛 답이 새 답을 덮지 않게** 순번(`SHEET_SEQ`)을 둔다. 없으면 「힐튼」을 지우고 「교토」를 쳤을 때
힐튼 결과가 뒤늦게 덮어쓴다.

**되돌리기** — 커밋 2개. 창고는 건드리지 않았다.

**🔴 §18 함정이 또 났다** — 검사 준비로 `cp r9/public/*.html` 을 돌리는 바람에
**방금 고친 `index.html` 이 옛 것으로 덮였다.** 올리기 전 확인(`assert`)이 잡아냈다.
→ **파일을 모아 검사할 때는 「고친 파일을 «맨 마지막에» 넣는다」.**

## 26. 2026-09-09 · 🔴 첫 화면에서 검색칸이 안 나오던 것

**왜** — 대표님: *«검색 적용이 바로 안 되었는데»*

§25 에서 `openSheet` 안에 입력칸을 넣었는데, 그 코드는 **`if (!box)` — 시트가 «없을 때만»** 도는 자리였다.
🔴 **첫 화면(`index.html`)에는 시트가 «미리 그려져» 있다.** 그래서 그 분기를 건너뛰고
**입력칸 없는 옛 시트**가 그대로 열렸다. 도시 화면은 shop.js 가 시트를 만들어서 «잘 나왔다» —
**그래서 내 확인(배포된 파일에 글자가 있나)에는 걸리지 않았다.**

| 곳 | 무엇을 |
|---|---|
| `shop.js` `openSheet` | 시트를 «어디서 만들었든» 입력칸이 없으면 `#all` 앞에 넣어준다 |

🔴 **`index.html` 의 정적 시트는 지우지 않았다.** 지우면 첫 화면이 시트를 만드는 순간이 늦어지고,
다른 화면과 동작이 갈린다. **부품은 `shop.js` 한 곳에 두고, 마크업은 있으면 쓰고 없으면 만든다.**
🔴 두 번 넣지 않는다 — `!document.getElementById('sheetq')` 조건이 막는다.

**배운 것 (같은 실수 반복 금지)**
🔴 **「배포된 파일에 그 글자가 있나」는 «충분한 확인이 아니다».**
글자는 있는데 **그 자리가 실행되지 않을 수 있다.** 화면마다 마크업이 다른 곳
(첫 화면 = 정적 · 나머지 = shop.js 생성)은 **양쪽을 따로 확인한다.**

## 27. 2026-09-09 · 🔴 검색 엔진 교체 (§43 · SEARCH_REBUILD.md)

**왜** — 대표님: *«검색 기능의 정확성이 떨어짐»* · *«글자를 적으니깐 한참 뒤에 비슷한 글자가 밑에 뜬다»*
실측: 정확히 친 이름이 1등이 아니었고(21번째), 붙여 쓰면 0곳, 영어로 치면 엉뚱한 것, **최대 4.34초**.

### 코드 — 전부 커밋으로 되돌아간다

| 파일 | 커밋 | 되돌리면 |
|---|---|---|
| `api/search.js` | `a5010fc` | 🔴 **통째로 바뀌었다.** 옛 것으로 되돌리면 옛 검색(느리고 부정확)으로 돌아갈 뿐, 다른 화면 영향 0 |
| `api/cron/learn.js` | `d422e17` | ⑥번 블록(`shop_rebuild_search`) 다섯 줄만 지우면 원복. 지우면 새 호텔이 검색에 안 들어온다 |
| `public/shop.js` | `31545d4` | `}, 150)` 를 `}, 350)` 으로 되돌리면 원복 (숫자 하나) |
| `vercel.json` | `regions:["hnd1"]` 두 줄 | 🔴 지우면 함수가 **미국(iad1)** 으로 돌아가 검색이 다시 0.3초쯤 느려진다. 요금은 어느 쪽이든 0원 |
| `sql/2026-09-09-search-engine.sql` (신설) | — | 창고에 들어간 것의 **사본**. 파일만 지워도 동작에 영향 없음 |

### 창고 — 표·함수 단위로 되돌린다

| 것 | 무엇 | 되돌리는 법 | 다른 곳 영향 |
|---|---|---|---|
| `shop_hotel_search` (신설 표) | 검색 전용 · 4,776줄 | `drop table shop_hotel_search cascade` | 🔴 **검색만 죽는다.** 다른 화면 0 |
| `search_hotels()` (신설) | 점수·정렬 | `drop function search_hotels` | 검색 창구가 오류 → 빈 결과(화면은 안 죽는다) |
| `shop_score()` · `shop_search_key()` (신설) | 점수·정규화 | `drop function …` | 위와 같음 |
| `shop_rebuild_search()` (신설) | 매일 갱신 | `drop function …` | 표가 안 늙지 않을 뿐 |
| `v_shop_card_lite` (신설 뷰) | v_shop_card 의 가벼운 판 | `drop view v_shop_card_lite` | **없음** — 기존 `v_shop_card` 는 그대로 살아 있다 |
| `pg_trgm` 확장 | 오타 허용 | 끄지 않는다 | 무료·다른 표에 영향 없음 |

🔴 **기존 `v_shop_card` 는 손대지 않았다.** 다른 화면 다섯 곳이 그것을 쓰고 있어서, 고치는 대신 **가벼운 판을 따로 만들었다.** 되돌릴 때 `v_shop_card` 를 볼 필요가 없다.

### 🔴 되돌릴 수 없는 것 (정직하게 적는다)

**없다.** 표·함수·뷰 전부 «새로 만든 것»이고, 기존 표는 한 칸도 바꾸지 않았다. 위의 것을 전부 지우고 `api/search.js` 를 옛 커밋으로 되돌리면 2026-09-08 상태와 완전히 같아진다.

### 배운 것 (같은 실수 반복 금지)

🔴 **「함수를 만들었으니 빠를 것」은 확인이 아니다.** 점수 함수 안에 «작은 질의»(`select … from unnest(…)`)를 두었더니 창고가 **4,776줄마다 그 질의를 따로 돌렸다 — 열쇠 하나에 90ms, 넷이면 346ms.** 무늬(`'%말%'`)를 바깥에서 한 번만 만들어 넘기니 **12.9ms** 가 됐다(**27배**). **함수를 만들면 `explain analyze` 로 한 번 재 본다.**

🔴 **느린 곳을 짐작하지 않는다.** 처음엔 「오타 계산이 비싸겠지」로 잡았는데 재보니 **아니었다**(536→402ms, 거의 그대로). 실제 범인은 위의 «작은 질의»였다. **3분이면 잰다.**

🔴 **함수가 도는 «자리»도 속도다.** 창구는 미국(iad1)에서 돌고 창고는 도쿄에 있었다 — 조회 한 번마다 태평양을 왕복했다. `vercel.json` 두 줄로 도쿄(hnd1)로 옮겼다. **요금 0원**(Hobby 요금제도 단일 지역은 고를 수 있다 — 공식 문서 확인).

## 28. 2026-09-09 · 첫 화면 맨 아래 「찾는 호텔이 없나요」 칸을 없앰

**왜** — 대표님: *«밑에 이부분은 필요없는거 아니야? 하나로 통합했으니까»*

§25 에서 검색칸을 위 「어디 가세요?」 안으로 합친 뒤, 이 칸에 남은 일은 **«위를 쓰세요»라고 말하는 것뿐**이었다.
그런데 그 「어디 가세요?」 줄은 `.stick`(`position:sticky;top:0`) 이라 **스크롤해도 화면 맨 위에 계속 붙어 있다** — 같은 단추가 이미 늘 손 닿는 곳에 있었다(실측 확인). 같은 일을 하는 입구가 둘이면 손님이 어느 쪽이 진짜인지 헷갈린다.

| 파일 | 되돌리는 법 | 영향 |
|---|---|---|
| `public/index.html` | 지운 자리에 **왜 지웠는지 주석**이 그대로 남아 있다. 커밋 하나 되돌리면 원복 | 첫 화면 맨 아래 칸 하나뿐. 다른 화면 0 |

🔴 **다시 넣지 마라.** 주석에도 적어 두었다.
🔴 **아래 여백은 그대로다** — `.foot` 의 `margin-top:32px` + `padding:24px…40px` 가 이미 만든다(칸의 `padding-bottom:24px` 에 기대고 있던 것이 아니다).
🔴 **함께 지우지 않은 것** — 도시 딱지 줄 끝의 `전체보기` 단추도 같은 시트를 연다. 그것은 «도시 목록의 연장»이라 자리에 맞고, 안내 문구가 아니라 **그 줄의 마지막 칸**이다. 남긴다.

## 29. 2026-09-09 · 검색이 「달」을 이해하게 + 「10만원 이하」 밑에 18만원이 섞이던 것

**왜** — 대표님: *«10월 10만원이라고 넣었는데 … 9월달 거 아무거나 나오고 8만원대 나왔다가 18만대 나오고»*

| 파일·창고 | 되돌리는 법 | 영향 |
|---|---|---|
| `api/search.js` | 커밋 하나 되돌리면 원복. 달 인식은 `parse()` 안 한 덩어리 | 달을 다시 못 읽게 될 뿐 |
| `public/search.html` | 딱지 한 줄 + 안내 두 덩어리 | 화면 문구만 |
| `search_hotels()` | 🔴 **인자를 가운데 끼워 넣었다.** 되돌리려면 `drop function search_hotels(…)` 후 사본(`sql/2026-09-09-search-engine.sql`)의 옛 판을 다시 만든다 | 창구는 **이름으로** 부르므로(`p_q=>…`) 순서가 바뀌어도 안 깨진다 |

🔴 **되돌릴 수 없는 것: 없다.** 표는 하나도 안 바꿨고, 창고 함수만 다시 만들었다.

**배운 것**
🔴 **창고 함수의 인자를 «가운데» 끼워 넣으면 순서로 부르던 곳이 전부 깨진다.** 시험 질의가 즉시 `function does not exist` 로 잡아냈다. **부르는 쪽은 반드시 이름으로 부른다**(`p_q=>…`) — 창구는 이미 그렇게 하고 있어 무사했다.
🔴 **조건을 「넓힐」 때 조건을 «없애지» 않는다.** 넓히기는 1.3배처럼 **정도가 있는 것**이어야 한다. 없애면 화면 딱지(「100,000원 이하」)와 목록이 어긋나 손님이 속았다고 느낀다.
🔴 **값 이름이 두 곳에 있으면 언젠가 갈린다.** 신상 값을 `shop_new_live` 에서 따로 읽고 있었는데, 실측해 보니 `shop_price_month` 의 첫 달과 **완전히 같았다**(3,498곳 차이 0). 한 곳으로 합쳤다.

## 30. 2026-09-09 · 검색 결과 맨 아래 「아고다에서 찾기」 → 「그 도시 추천 상품」

**왜** — 대표님: *«이거는 넣지 말자»* · *«아고다에서 찾기 넣지 말고, 후쿠오카 추천상품을 보여주는 게 맞어»*

찾던 숙소를 못 찾은 손님을 **남의 집으로 보내는 대신 우리가 가진 것을 내민다.**
🔴 이것은 **D-114 의 「2층 = 아고다 뒷문」을 검색 화면에서 내리는 것**이다 → **D-120** 으로 갱신.

| 파일 | 되돌리는 법 | 영향 |
|---|---|---|
| `public/search.html` | 커밋 하나. 지운 자리에 **왜 지웠는지 주석**이 남아 있고, `agodaLink()` 함수도 **함께 지웠다**(남겨두면 다음 사람이 「살아 있는 길」로 오해한다) | 검색 결과 화면 맨 아래 한 칸뿐 |

🔴 **부품을 새로 만들지 않았다** — 세부 화면이 쓰는 `shop.js` 의 `mountCityProducts` 를 그대로 불렀다. 화면마다 복사하면 화면마다 달라진다.
🔴 **도시를 못 알아들은 검색(예: 「일본 10만원 3성급」)에서는 칸 자체를 넣지 않는다** — 어느 도시 상품인지 말할 수 없기 때문이다.
🔴 **상품이 0개인 도시면 칸과 구분선이 함께 사라진다**(`mountCityProducts` 가 `previousElementSibling` 까지 지운다). 그래서 구분선을 칸 **바로 앞**에 두었다 — 사이에 다른 것을 끼우면 엉뚱한 것이 지워진다.

**되돌릴 수 없는 것: 없다.**

**잃는 것 (정직하게)** — 숙소를 못 찾은 손님이 아고다로 이어서 검색하고 예약하면 붙던 수수료 길 하나가 없어진다. 대신 그 자리에 **KKday 추천 상품**(cid 15352)이 들어가므로 수익 길이 사라지는 것이 아니라 **바뀌는** 것이다. 다시 넣고 싶으시면 한 줄이면 된다.

## 31. 2026-09-09 · 예산을 넘기지 않게 + 「가장 가까운 곳」·「성급 한 단계 아래」 신설

**왜** — 대표님: *«15만 이하인데 17만원 19만원대 호텔이 뜸»*

| 파일·창고 | 되돌리는 법 | 영향 |
|---|---|---|
| `search_hotels()` | 🔴 **통째로 다시 만들었다**(인자는 그대로). 사본 `sql/2026-09-09-search-engine.sql` 의 옛 판으로 `create or replace` | 돌려주는 것에 `near`·`alt_star`·`alt_count` 가 **늘었다**. 옛 판으로 돌아가면 화면의 두 칸이 저절로 사라진다(오류 아님 — 빈 배열로 처리) |
| `api/search.js` | 커밋 하나. `near`·`alt_star`·`alt_count` 를 넘겨주는 세 줄 | — |
| `public/search.html` | 커밋 하나. 「가장 가까운 곳」 칸 + 「성급 한 단계 아래」 단추 | 화면 두 칸 |

🔴 **되돌릴 수 없는 것: 없다.** 표는 하나도 안 바꿨다.

**배운 것**
🔴 **「범위를 넓힌다」가 손님에게 도움이 된다고 짐작하지 않는다.** 실측해 보니 넓히기는 정보를 더 준 게 아니라 **지우고 있었다** — 「146원만 넘는 곳」이 「44,226원 넘는 곳」과 뒤섞여 묻혔다. **섞지 말고 나눠서 보여준다.**
🔴 **아무것도 안 넓혔는데 「넓혔습니다」라고 말하고 있었다.** 2회차를 «돌기만 하면» 깃발이 켜졌기 때문이다. **결과가 실제로 늘어났을 때만** 그렇게 말한다.
🔴 **「없습니다」로 끝내지 않는다** — 대표님 상시 지시. 가장 가까운 것과 다음 길을 함께 준다.

## 32. 2026-09-09 · 숙소 유형 이름표 (료칸·아파트·빌라·호스텔·리조트)

**왜** — 대표님: *«야놀자에서는 여기 여관이라고 표시된다. 사람들이 오해함»* · *«바로 확인된 것만 일단 이름을 달아»*

| 것 | 되돌리는 법 | 영향 |
|---|---|---|
| 창고 `shop_guess_type()` | `drop function shop_guess_type` | 갱신 함수가 오류 → 유형이 안 채워질 뿐 |
| `shop_hotel_search.property_type` (칸 추가) | 칸을 두어도 무해. 지우려면 `alter table … drop column` | 화면에서 이름표만 사라짐 |
| `shop_rebuild_search()` · `search_hotels()` | 사본 `sql/2026-09-09-search-engine.sql` 의 옛 판으로 다시 만든다 | — |
| `public/shop.js` `typeTag()`·`PTYPE` | 커밋 하나. 부품 한 곳 + 호출 2곳 | 이름표만 사라짐 |
| `public/shop.css` `.ptype` | 규격 한 줄 | — |
| `public/search.html` `ptype:` 2곳 | 커밋 하나 | — |

🔴 **되돌릴 수 없는 것: 없다.** 기존 표·칸을 하나도 바꾸지 않았다.

**배운 것**
🔴 **모르는 것을 「호텔」로 채우지 않는다.** 이름에 단서가 없으면 null 을 주고 화면에 아무것도 안 붙인다. 3,723곳이 여기 해당한다.
🔴 **추측 잣대로 손님 결과를 지우지 않는다.** 「브랜드가 있으면 진짜 호텔」은 임페리얼 호텔 오사카(브랜드 비어 있음·378실)가 반증했다 — 대표님 지적으로 폐기(§53).
🔴 **우리 화면에서 「아고다」를 입에 올리지 않는다** — 누르면 아고다로 넘어가는 구조이기 때문이다(대표님 2026-09-09).

## 33. 2026-09-09 · 🔴 「어디로 가세요?」 시트에 검색칸이 없던 것 — §26 함정이 그대로 다시 났다

**왜** — 대표님: *«어떤 식으로 전체보기 했을 때 검색창이 없어. 여기서도 검색 또는 선택을 할 수 있게 되어 있어야지»*

**원인** — 🔴 **`list.html`(도시 화면)이 자기만의 `openSheet()` 를 만들어 `shop.js` 것을 덮고 있었다.**
그래서 shop.js 가 검색칸을 넣는 코드에 **아예 도달하지 못했다.** 도시 화면에서는 시트에 검색칸이 **처음부터 없었다.**

🔴 **§26 에서 겪은 「부품 복사」 함정이 «그대로» 다시 났다.** 그때 배운 것을 못 지켰다.

**왜 검사기가 못 잡았나** — `scripts/check.mjs` 의 이름 겹침 검사가
`^(const|let|function)` 만 봤는데 shop.js 쪽은 **`async function openSheet`** 라 **명부에 안 들어갔다.**
→ 검사기에 `async` 를 넣어 고쳤다. **같은 구멍이 다른 이름에도 있었을 수 있다.**

| 파일 | 되돌리는 법 | 영향 |
|---|---|---|
| `public/shop.js` | `paintSheet` 에 「다른 나라·전체 보기」 추가 · 글자 검색 대상을 `ALLCITIES` 로 · `openSheet` 에 `warmAllCities()` · `showAllCities` 에 검색어 유지 | 커밋 하나 |
| `public/list.html` | 지운 자리에 **왜 지웠는지 주석**이 남아 있다 | 커밋 하나 |
| `scripts/check.mjs` | `async function` 을 이름 명부에 포함 | 커밋 하나 |

🔴 **되돌릴 수 없는 것: 없다.**

**배운 것**
🔴 **화면이 shop.js 와 «같은 이름의 함수»를 만들면 부품이 죽는다.** 검사기를 믿되, 검사기가 못 보는 모양(`async`)이 있었다.
🔴 **「전체 보기」로 펼친 뒤에도 검색칸은 살아 있어야 한다** — 입력칸을 `#all` 의 «형제»로 두었기 때문에 목록만 갈아 끼워도 남는다.
🔴 **글자를 치면 「전체 보기」를 누르지 않아도 전 나라에서 찾는다** — 단추를 누르게 만드는 것보다 치는 대로 찾아 주는 것이 낫다.

## 34. 2026-09-10 · 「값 변동」 호텔 목록을 미리 만든 표로

**왜** — 대표님: *«호텔값이 왜 또 안 되는 거야?»* 원인은 조건이 아니라 뷰가 **4.57초**라 창구가 시간 초과된 것(§55).

| 것 | 되돌리는 법 | 영향 |
|---|---|---|
| `shop_price_habit` (신설 표) | `drop table shop_price_habit` | 관리자 「값 변동」 호텔 목록만 다시 0 이 된다. 손님 화면 영향 **0** |
| `shop_rebuild_price_habit()` | `drop function …` | 표가 안 늙지 않을 뿐 |
| `api/cron/learn.js` ⑦ | 네 줄 삭제 | — |
| `api/admin/index.js` | 표 이름 3곳을 `v_shop_price_habit` 로 되돌리면 원복 | 다시 느려진다 |

🔴 **되돌릴 수 없는 것: 없다.** 기존 뷰 `v_shop_price_habit`·`v_shop_price_move` 는 **그대로 살아 있다** — 표를 만들 때만 쓴다.

**배운 것**
🔴 **96만 줄짜리 `shop_price_daily` 를 «누를 때» 훑는 구조를 만들지 마라.** 검색(§43)·점수 함수(§43)에 이어 **세 번째**다.
🔴 **`.catch(() => [])` 가 원인을 숨긴다.** 목록이 비면 「없다」가 아니라 「못 가져왔다」를 먼저 의심하고 시간을 잰다.

## 35. 2026-09-10 · 관리자 로딩 개선 (왕복 묶기)

**왜** — 대표님: *«관리자 페이지가 전체가 로딩이 늦어»*. 전수 측정 결과 창고 질의는 전부 141ms 이하였고, 원인은 **창구가 창고를 순차로 여러 번 왕복**한 것이었다(§56).

| 것 | 되돌리는 법 | 영향 |
|---|---|---|
| `shop_admin_counts()` (신설) | `drop function shop_admin_counts` | 「한눈에」 개수만 비게 된다. 다른 탭 영향 0 |
| `shop_timeit()` (신설 · 측정 도구) | 두어도 무해 — **남겨 둔다**. 다음에 느릴 때 쓴다 | 없음 |
| `api/admin/index.js` | 커밋 하나. 「한눈에」·「호텔」·「회원」·「KKday」 네 곳 | 다시 느려질 뿐 |

🔴 **되돌릴 수 없는 것: 없다.** 표를 하나도 안 바꿨고, 화면(`admin.html`)도 손대지 않았다.

**배운 것**
🔴 **느리면 먼저 «전수로» 잰다.** `shop_timeit(array[...])` 한 번이면 관리자 질의 15개를 한꺼번에 잰다.
🔴 **서로 상관없는 질의를 «순차로» 기다리지 않는다.** 한눈에가 개수 7개를 따로 세느라 8번 왕복하고 있었다.
🔴 **개수는 창구에서 세지 말고 창고 함수 하나로 센다** — 늘릴 때도 한 곳만 고치면 된다.

## 36. 2026-09-10 · 관리자 사진 — 순서 지정 · 대표 사진이 손님 화면에 «제대로» 반영

**왜** — 대표님: *«사진을 올리고 보여지는 순서를 지정해줄 수 있게 하면 좋겠어»* ·
*«지금 썸네일이 가져온 것이 전경이 아니고 이벤트 같은 사진들이 메인인 것도 있더라구 — 업로드하여 1번 사진으로 선택하면 변경되게»* ·
*«호텔의 경우 전체가 없어»* · *«여기 사진도 우리 서버에 저장되고 있는 거야»*

### 🔴 고치다 발견한 진짜 버그

**대표 사진을 바꿔도 «신상 호텔» 손님 화면은 그대로였다.**
`photo_main` 이 `shop_hotel` 과 `shop_new_pool` 만 바꾸고 **`shop_new_live` 를 빠뜨렸다.**
신상 호텔의 손님 화면은 `shop_new_live` 를 본다 — 즉 대표를 바꿔도 **손님에게는 옛 사진이 계속 나갔다.**
→ 부품 `setHotelPhoto(hid, p)` 를 만들어 **`shop_hotel`·`shop_new_live`·`shop_new_pool`·`shop_hotel_search` 네 곳을 함께** 바꾼다.

### 정한 것 — 🔴 **맨 앞 사진 = 손님이 보는 사진**

순서와 대표를 «따로» 두면 관리자가 보는 차례와 손님이 보는 사진이 갈린다. **하나로 묶었다.**
화면에서 `◀ ▶` 로 옮기거나 「1번으로」를 누르면 그 자리에서 저장되고 손님 화면까지 바뀐다.

| 것 | 되돌리는 법 | 영향 |
|---|---|---|
| 창구 `photo_order` (신설 act) | 그 블록 삭제 | 순서 바꾸기만 사라짐 |
| 창구 `setHotelPhoto()` (신설 부품) | 옛 방식(세 곳 개별 PATCH)으로 되돌림 | 🔴 되돌리면 **신상 손님 화면이 다시 안 바뀐다** |
| `public/admin.html` | 커밋 하나 — `movePhoto`·`firstPhoto`·`savePhotoOrder`·`srcName` | 화면 단추만 |
| 호텔·사진 「전체(소개+신상)」 | 창구 `kind==='all'` 블록 + 화면 `<option>` | 갈래 하나 |

🔴 **되돌릴 수 없는 것: 없다.** 표·칸을 하나도 바꾸지 않았다(`sort_order`·`is_main` 은 원래 있던 칸).

### 사진이 어디 있는지 (실측 · 대표님 질문)

| | 결과 |
|---|---|
| 사진표 `shop_hotel_photo` 1,074장 | 🔴 **전부 우리 창고**(Supabase 저장소) · 아고다 주소를 가리키는 사진 **0장** |
| 신상 호텔 3,705곳 · 소개 1,074곳 | 대표 사진 **전부 우리 창고** |
| 🔴 화면에 보이던 `pix5.agoda.net…` | **사진 주소가 아니라 `source`(어디서 가져왔나 기록) 칸이었다.** 오해를 부르므로 **「아고다에서 가져옴」·「직접 올림」** 으로 바꿨다 |

### 사진 올리기 한도 (실측)

**장수 제한 없음**(여러 장 골라 순서대로 올라감) · **한 장 약 18MB**(창구 25MB · base64 포장분 감안) ·
올리면 **두 벌 자동 생성**(상세 2000px q82 · 목록 800px q80 · JPEG).

### 🔴 아직 안 한 것

**호텔당 사진이 «1장뿐»이다**(1,074곳 × 1장). 대표님: *«사진을 좀 많이 보여주면 좋기 하잖아»*.
아고다는 호텔당 여러 장을 준다. 다만 **전량에 걸리는 규격 변경**(호텔 4,780곳 × 5장 = 약 24,000장, 두 벌이면 48,000개 파일)이라
🔴 **저장 용량·월 비용을 먼저 재서 보고한 뒤 진행한다.**

## 37. 2026-09-10 · 뒤로가기가 «구글로 나가던 것» · 사진 10장 제한 · 사진 슬라이드

**왜** — 대표님: *«뒤로가기 버튼 누르면 구글로 넘어간다. 이 부분 내가 할 때마다 나오는 것 같애»* ·
*«사진 등록은 최대 10장 정도는 등록할 수 있게 하는 게 맞지 않을까?»* ·
*«화살표 없이 슬라이드 형태로 넘겨서 볼 수 있게. 화살표 생기면 이상함»*

### 🔴 뒤로가기 — `referrer` 만으로는 못 막는다

옛 방식은 *«직전 화면이 우리 사이트면 `history.back()`»* 이었다. 그런데 **주소를 직접 열거나 새 창(`target=_blank`)으로 연 경우**
referrer 는 우리 host 인데도 **그 «창»의 이동 기록에는 우리 화면이 없어서** 뒤로가 검색 결과로 나가버린다.
관리자 「화면」 단추가 새 창을 여는데, 대표님이 겪으신 것이 정확히 이 경로다.

**고침** — 🔴 **«이 창에서 우리 화면을 몇 번 거쳤는지»를 직접 센다**(`sessionStorage.tw_depth`, `mountChrome` 이 한 번씩 올린다).
한 번뿐이면 **뒤로 가지 않고** `window.UP_URL || '/'` 로 보낸다. 🔴 **우리 사이트 밖으로 내보내지 않는다.**
창을 닫으면 사라지는 기록이라 손님을 따라다니지 않는다.

### 사진 — 한 호텔 10장

| 곳 | 무엇 |
|---|---|
| `api/admin/upload.js` | `MAX_PHOTOS = 10` · 올리기 전에 지금 장수를 세어 거절 |
| `public/admin.html` | 같은 수를 두고 **고르는 즉시** 안내(«3장만 올릴 수 있습니다») — 다 올린 뒤 거절당하면 손해다 |

🔴 **두 곳에 같은 수가 있다.** 바꿀 때 한 곳만 고치면 갈린다. 화면만 막으면 주소로 직접 부를 때 뚫린다.
**10장인 이유** — 손님이 옆으로 밀어 보는 슬라이드는 열 장이 넘으면 끝까지 안 본다.

### 사진 슬라이드 (화살표 없이)

격자 + `◀ ▶` 단추 → **가로 한 줄 슬라이드**(밀어서 넘겨 봄) + **끌어서 놓으면 순서 변경**.
끄는 카드는 흐려지고 놓을 자리에 테두리가 뜬다. 저장은 **놓는 순간 한 번**만.

| 파일 | 되돌리는 법 | 영향 |
|---|---|---|
| `public/shop.js` `goBack`·`twStep` | 커밋 하나 | 🔴 되돌리면 뒤로가 다시 사이트 밖으로 나간다 |
| `api/admin/upload.js` | `MAX_PHOTOS` 블록 삭제 | 제한만 사라짐 |
| `public/admin.html` | 커밋 하나 (`.pgrid` 규격 · 끌기 부품 · 제한 안내) | 화면만 |

🔴 **되돌릴 수 없는 것: 없다.**

**배운 것**
🔴 **`document.referrer` 는 «그 창의 이동 기록»을 말해 주지 않는다.** 새 창·주소 직접 열기에서 갈린다 — 창 안에서 «우리가 직접 센 횟수»가 유일하게 확실하다.
🔴 **막을 것은 화면과 창구 «둘 다»에서 막는다.** 화면만 막으면 주소로 부를 때 뚫린다.

## 38. 2026-09-10 · 🔴 클로드가 «만든» 오류 — 호텔 자세히가 「불러오는 중」에서 멈춤

**왜** — 대표님: *«호텔 값 자세히 보면 불러오는 중이라고 안 뜸»*

🔴 **내가 만든 오류다.** 상품 화면에 날짜 부품이 없어서 공용으로 빼내면서 **`dt`·`dw` 를 `md`·`wd` 로 이름을 바꿨는데,
호텔 자세히 화면의 표(잰 날 머리글·숙박일 칸)에서 쓰던 **`dw(` 두 곳을 못 고쳤다.**
`dw` 가 사라지자 그리다 멈췄고, 그 화면은 오류를 잡는 곳이 없어 **「불러오는 중…」 그대로 굳었다.**

**창고는 멀쩡했다** — 자세히 보기가 쓰는 질의 셋을 재보니 **2.3ms · 1.3ms · 0.4ms** 였다.

### 고친 것 둘

| | 무엇 |
|---|---|
| 이름 | 호텔 표의 `dt(c)`·`dw(c)`·`dt(st)`·`dw(st)` → `md`·`wd` |
| 🔴 **안전망** | 단추로 여는 화면(`pmHotel`·`openPhotos`)은 `boot()` 의 오류 처리 «밖»이라 멈추면 그대로 굳는다. **`safeView()` 로 감싸 오류를 화면에 그대로 띄운다** |

| 파일 | 되돌리는 법 | 영향 |
|---|---|---|
| `public/admin.html` | 커밋 하나 | 화면만 |

🔴 **되돌릴 수 없는 것: 없다.**

### 🔴 배운 것 — 되풀이 금지

**부품 «이름»을 바꿀 때는 쓰는 곳을 전부 세어 본다.** 나는 `dt(su.best.stay)` 같은 «요약 칸»만 고치고
표 안의 `dw(c)` 를 놓쳤다. 바꾸기 전에 `grep -c` 로 «몇 곳에서 쓰는지»를 세고, 바꾼 뒤 **0이 되는지 확인**한다.

**검사기는 이것을 못 잡는다.** `dw` 는 «선언되지 않은 이름»이라 문법 오류가 아니라 «실행할 때» 터진다.
🔴 그래서 **단추로 여는 모든 화면에 안전망을 둔다** — §55 의 *«.catch 가 원인을 숨긴다»* 와 같은 교훈이다.

## 39. 2026-09-10 · 🔴 «왜 자꾸 같은 문제가 생기나» — 「값 변동」 화면이 상태를 안 지우던 것

> 대표님: *«상품에서 일자별을 누르면 상단에 상품 개수가 0으로 뜬다. 이거 잘못 연결된 거 아니야. 전체를 확인해.
> 이 상태에서 값 변동을 눌러도 목록 화면으로도 안 가고 세 번째 이미지로 된다. 왜 자꾸 같은 문제가 생기는 거야.»*

### 뿌리는 하나였다

「값 변동」 탭은 화면이 하나가 아니라 **셋**이다 — **목록 / 호텔 자세히 / 상품 자세히**.
어느 것을 그릴지는 전역 변수 `PM_PID`(상품)·`PM.hid`(호텔)가 정한다. 그런데 —

| 증상 | 원인 |
|---|---|
| 상단 개수가 **0** | 개수(`total`·`product_total`)를 **«목록 응답에만»** 담았다. 자세히 보기 응답에는 없어 화면이 0으로 그렸다 |
| 메뉴 「값 변동」을 눌러도 **자세히 화면** | `go(tab)` 이 `PM_PID`·`PM.hid` 를 **안 지웠다.** 늘 마지막에 보던 화면으로 돌아갔다 |
| 「호텔/상품」 단추를 눌러도 마찬가지 | 그 단추도 `PM_TAB` 만 바꾸고 자세히 상태를 안 지웠다 |

🔴 **셋 다 «상태를 지우지 않았다»는 한 가지 원인이다.** 증상이 여러 개로 보였을 뿐이다.

### 고친 것

| 곳 | 무엇 |
|---|---|
| `go(t)` | 메뉴를 누르면 그 탭의 **자세히 보기 상태를 지운다**(`PM_PID`·`PM.hid`·쪽 번호). 메뉴를 누르는 것은 «처음부터 보겠다»는 뜻이다 |
| `pmSeg(t)` (신설) | 「호텔/상품」 갈래를 바꿔도 **목록으로** 돌아간다 |
| 창구 `hid`·`pid` 분기 | 🔴 **개수를 함께 보낸다** — 개수는 «어느 화면에서나 같아야» 한다 |

| 파일 | 되돌리는 법 | 영향 |
|---|---|---|
| `public/admin.html` · `api/admin/index.js` | 커밋 각 하나 | 관리자 「값 변동」만 |

🔴 **되돌릴 수 없는 것: 없다.**

### 🔴 배운 것 — 같은 문제가 되풀이된 진짜 이유

**«한 탭 = 한 화면»이 아니면 상태를 반드시 «들어올 때» 지운다.**
화면이 여럿인 탭을 만들 때는 (1)메뉴로 들어올 때 (2)갈래를 바꿀 때 **둘 다** 초기화한다.
그리고 **머리글에 쓰는 숫자(개수)는 모든 분기에서 같이 보낸다** — 한 분기에만 담으면 다른 화면에서 0이 된다.

## 40. 2026-09-10 · ✅ 사진 전량 WebP 전환 완료 (4,824곳)

**대표님 승인** — 원본과 바꾼 것을 직접 보시고: *«차이를 못 느끼겠는데. 괜찮은데»* → *«지금 바꿔»*

### 확정 규격 (🔴 이제 이것이 정본이다)

**상세 1600px WebP q78 · 목록 800px WebP q75.** 올리기 창구(`api/admin/upload.js`)도 이 규격으로 맞춰야 한다.

### 결과 (실측)

| | 전 | 후 |
|---|---|---|
| 상세용 | 370 KB (2000px JPEG) | **186 KB** (**50% 줄어듦**) |
| 목록용 | 147 KB | **59 KB** (**60% 줄어듦**) |
| 전환한 곳 | — | **4,824곳** |
| 아낀 용량 | — | **865 MB** |
| 하루 1,000명 기준 월 트래픽 | 490 GB · 약 3만원 | **약 244 GB · 0원** |

### 되돌리는 법

| 것 | 어떻게 |
|---|---|
| 🔴 **원본 JPEG** | **하나도 지우지 않았다.** `{id}/big.jpg`·`thumb.jpg` 가 그대로 있다 |
| 되돌리기 | `update … set photo_url = replace(photo_url,'.webp','.jpg')` 를 `shop_hotel`·`shop_new_live`·`shop_new_pool`·`shop_hotel_search`·`shop_hotel_photo` 다섯 곳에 |
| `api/cron/webp.js` · `shop_webp_todo` · `shop_webp_build/next` | 두어도 무해. 다음에 또 쓸 수 있다 |
| `vercel.json` 임시 크론 | 🔴 **끝나고 지웠다**(확인함 · 남은 크론 6개) |

### 🔴 배운 것 — 되풀이 금지

1. **「다시 세우기」 일꾼이 바꾼 값을 되돌린다.** 전환 중 `shop_rebuild('live')`(매시 5·35분)가 돌면서
   `shop_new_live` 의 사진 주소를 **옛 값으로 덮어썼다**(990곳). 파일은 다 만들어졌는데 화면만 옛 것이었다.
   🔴 **전량 작업을 할 때는 그 값을 다시 세우는 일꾼이 있는지 먼저 본다.** 끝난 뒤 반드시 다시 확인한다.
2. **원본 파일 이름이 한 가지가 아니다.** `big.jpg` 말고 `main.jpg`(1,043곳)·`thumb.png`(12곳)도 있었다.
   🔴 **하나만 찾으면 조용히 빠진다.** 후보를 여러 개 두고, 끝난 뒤 «남은 것 0» 을 숫자로 확인한다.
3. **동시에 돌리려면 몫을 나눠야 한다.** `FOR UPDATE SKIP LOCKED` 로 각 일꾼이 다른 줄을 집게 했다.
   덕분에 2시간 20분 예상이 **약 25분**에 끝났다.

## 41. 2026-09-10 · ✅ 2단계 — 사진 여러 장 담기 + 손님 화면에서 넘겨 보기

**대표님 지시** — *«사진을 좀 많이 보여주면 좋기 하잖아»* · *«화살표 없이 슬라이드 형태로»*

### 한 것

| 곳 | 무엇 |
|---|---|
| `api/cron/photo-import.js` (신설) | 갤러리 주소를 받아 **우리 창고에 파일로** 담는다. 규격은 §40 과 «같다»(1600px/800px WebP). 🔴 한 호텔 **10장까지** · 이미 담은 원본은 건너뛴다 |
| 담은 결과 | 🔴 **36곳 × 10장 = 356장** (실패 0). 순서는 **외관 → 객실 → 시설 → 그밖 → 주변** — 첫 장이 「전경」이 되게 |
| `api/h.js` · `api/hotel.js` | 상세 창구가 **사진 10장까지** 함께 준다(`is_main` → `sort_order` 차례) |
| `public/h.html` · `public/hotel.html` | 좌우로 밀어 넘기는 슬라이드 · **화살표 없음** · 아래 **점**이 몇 번째인지 알린다 |
| `public/shop.css` | `.phs`·`.phdot` 규격 (`.hero`·`.ph` 두 칸 모두) |

🔴 **사진이 한 장뿐인 호텔은 지금과 «똑같이» 보인다** — 화면이 스스로 가른다(실측: 힐튼 다낭 1장 → 옛 모양).

### 되돌리는 법

| 것 | 어떻게 | 영향 |
|---|---|---|
| 담은 사진 356장 | `update shop_hotel_photo set deleted_at=now() where url like '%/g-%'` | 대표 사진은 그대로 · 슬라이드만 사라짐 |
| 창구·화면 | 커밋 각 하나 | 사진 한 장만 보이던 옛 모양 |
| `api/cron/photo-import.js` | 두어도 무해 — 대표님이 더 모으시면 그대로 쓴다 |

🔴 **되돌릴 수 없는 것: 없다.**

### 🔴 다음에 이어서 할 때

대표님이 갤러리에서 주소를 더 모으시면 **같은 명령을 다시 돌리기만** 하면 된다.
이미 담은 원본은 건너뛰므로 **두 번 담기지 않는다.**
창고 A `blog_hotel_photo_url` 이 주소 정본 · 담긴 사진은 창고 C `shop_hotel_photo`.

### 배운 것

🔴 **손님이 실제로 보는 파일을 먼저 확인한다.** 처음에 `hotel.html` 을 고쳤는데,
`/h/{번호}` 는 **`h.html` + `api/h.js`** 를 쓴다. 두 벌이 있어 엉뚱한 쪽을 고칠 뻔했다.
**주소를 눌러 보고 나서 고친다.**

## 42. 2026-09-10 · 갤러리에서 «사진을 보고 순서대로» 골라 담기

**대표님 지시** — *«다운 받은 사진의 해당 호텔별로 전체 목록을 제공해서 내가 사진을 보고 순서대로 선택하면 등록할 수 있게.
리스트에는 사진이 있음을 표시해주면 좋겠어. 그래야 서버에 사진이 있는 곳은 내가 선택할 수 있으니깐.»*

### 만든 것

| 곳 | 무엇 |
|---|---|
| 창고 `shop_photo_pool` (신설) | 갤러리 주소 **4,195장 · 호텔 40곳**. 🔴 **정본은 창고 A `blog_hotel_photo_url`** — 여기는 사본이다(샵 창구가 창고 A를 못 본다). **여기서 고치지 않는다** |
| 창고 `v_shop_photo_ready` (신설 뷰) | 호텔별 「갤러리 몇 장 · 이미 몇 장 담았나」 한 줄 |
| 창구 `tab=pool` | 한 호텔의 갤러리 사진 목록. **이미 담은 것은 `saved:true`** 로 표시 |
| 창구 `act=photo_pick` | 고른 «차례대로» 담는다. 🔴 실제 저장은 `api/cron/photo-import.js` 를 부른다 — **규격(1600px/800px WebP)이 두 곳에서 갈리지 않게** |
| 화면 「호텔·사진」 목록 | 🔴 **「갤러리 N장」 칸** — 담을 수 있는 곳을 초록으로. 있는 곳에만 **「고르기」 단추** |
| 화면 「사진 고르기」 | 사진 격자 · **외관/객실/시설/주변/그밖 갈래 단추** · 누르면 **1,2,3… 번호**가 붙고 그 차례로 담긴다 · 이미 담은 것은 흐리게 + 「담김」 |

### 🔴 지킨 규칙

- **누르는 차례 = 손님이 보는 차례.** 맨 앞이 대표 사진이 된다
- **이미 담은 사진은 다시 못 고른다** — 두 번 담기지 않는다
- **한 호텔 10장까지** — 화면·창구 양쪽에서 막는다(같은 수를 쓴다)
- **파일 저장·변환은 부품 한 곳**(`photo-import.js`) — 관리자 창구가 그것을 부른다

### 되돌리는 법

| 것 | 어떻게 | 영향 |
|---|---|---|
| `shop_photo_pool` · `v_shop_photo_ready` | `drop` | 목록의 「갤러리 N장」과 고르기 화면만 사라짐 |
| `api/admin/index.js` · `public/admin.html` | 커밋 각 하나 | 관리자 화면만 |

🔴 **되돌릴 수 없는 것: 없다.** 손님 화면은 건드리지 않았다.

### 🔴 앞으로 (대표님이 사진을 더 모으시면)

정본(창고 A)이 늘면 **샵 창고로 다시 옮겨야** 한다 — 지금은 클로드가 손으로 옮겼다.
🔴 **다음에 할 일**: 이 옮기기를 `api/cron/learn.js` 에 붙여 **매일 자동**으로 돌게 한다.

## 43. 2026-09-10 · 🔴 로봇 42명 시간표 정리 — 같은 창고에 «동시에» 들어가던 것을 흩음

**대표님 지시** — *«로봇들 전체 체크하고 겹쳐서 문제가 안 생기는 시간에 따라»*

### 잰 것 (세 서비스 vercel.json 전수)

| | |
|---|---|
| 로봇 | **42명** (샵 6 · 스튜디오 18 · 블로그 18) |
| 🔴 **창고 A(블로그+스튜디오 공용)에 2명 이상이 «동시에» 들어가는 시각** | **하루 114회** |
| 가장 나쁜 시각 | 매시 **:00** — `yt-views`·`kw-survey`·`bot-read` + 그 시각의 하루 로봇 = **4명 동시** |

🔴 **창고 A 는 블로그와 스튜디오가 «함께 쓴다».** 방어선은 **분당 60회**이고, 여러 로봇이 같은 분에 시작하면 그것을 칠 수 있다.
샵(창고 C)은 별개라 겹쳐도 무해하지만, **같은 창고끼리는 흩어야 한다.**

### 옮긴 것 (13개 · 하루 1회짜리는 «이유가 붙어 있어» 되도록 그대로 뒀다)

| 로봇 | 전 | 후 | 왜 |
|---|---|---|---|
| `tw yt-views` | `0 * * * *` | `2 * * * *` | :00 에 4명이 몰렸다 |
| `tw kw-survey` | `0 * * * *` | `8 * * * *` | 〃 |
| `tw hotel-fill` | `30 * * * *` | `32 * * * *` | :30 에 5명 |
| `tw hotel-addr-fill` | `40 * * * *` | `42 * * * *` | :40 에 3명 |
| `tw kw-audit` | `15 5` | `17 5` | `bot-agoda-review`(:15) |
| `blog cron-collect` | `35 * * * *` | `37 * * * *` | — |
| `blog cron-photo` | `50 * * * *` | `52 * * * *` | — |
| `blog bot-official` | `33 * * * *` | `27 * * * *` | — |
| 🔴 `blog bot-read` | `*/10` | **`4-59/10`** | **정각(0·10·20…)을 피한다** — 하루 로봇들이 전부 정각에 있다 |
| `blog cron-build?check` | `25 * * * *` | `23 * * * *` | `push-to-shop`(:25) |
| `blog worker-audit` | `45 22` | `47 22` | `bot-rakuten-review`(:45) |
| `shop warm` | `30 */6` | `28 */6` | 같은 창고 `collect`(:30) |
| `shop learn` | `30 16` | `32 16` | 〃 (한국 01:32) |

### 결과

🔴 **같은 창고 동시 실행 — 하루 114회 → 0회.** 한 시각 최대 4명 → **1명**.
세 사이트 모두 정상(200) 확인.

### 🔴 다음에 로봇을 새로 만들 때

**정각(:00·:10·:20·:30·:40·:50)을 피한다.** 하루 1회짜리 로봇이 거의 다 정각에 있다.
새 로봇은 **:02·:08·:17·:23·:27·:37·:42·:47·:52** 처럼 «비어 있는 분»에 넣는다.
전수 확인은 세 `vercel.json` 을 모아 **분·시각을 함께** 펼쳐 보면 된다(분만 보면 과대평가된다 — 실제로 :00 은 12개로 보였지만 시각을 맞추면 4개였다).

### 지금 시간표 전체

| 서비스 | 로봇 | 크론(UTC) | 한국시간 |
|---|---|---|---|
| 블로그(창고 A) | `/api/bot-agoda-review?limit=5` | `15 * * * *` | 매시 15분 |
| 블로그(창고 A) | `/api/bot-basic?limit=2` | `13 2,8,14,20 * * *` | 한국 11:13,17:13,23:13,05:13 |
| 블로그(창고 A) | `/api/bot-official?limit=5` | `27 * * * *` | 매시 27분 |
| 블로그(창고 A) | `/api/bot-plan` | `30 15 1 * *` | 한국 00:30 |
| 블로그(창고 A) | `/api/bot-rakuten-review?limit=3` | `45 * * * *` | 매시 45분 |
| 블로그(창고 A) | `/api/bot-read` | `4-59/10 * * * *` | 매시 4-59/10분 |
| 블로그(창고 A) | `/api/bot-recheck` | `25 22 * * *` | 한국 07:25 |
| 블로그(창고 A) | `/api/cron` | `5 * * * *` | 매시 5분 |
| 블로그(창고 A) | `/api/cron-build?step=build` | `55 * * * *` | 매시 55분 |
| 블로그(창고 A) | `/api/cron-build?step=check` | `23 * * * *` | 매시 23분 |
| 블로그(창고 A) | `/api/cron-collect` | `37 * * * *` | 매시 37분 |
| 블로그(창고 A) | `/api/cron-daily` | `10 21,9 * * *` | 한국 06:10,18:10 |
| 블로그(창고 A) | `/api/cron-google` | `20 1,7,13,19 * * *` | 한국 10:20,16:20,22:20,04:20 |
| 블로그(창고 A) | `/api/cron-photo` | `52 * * * *` | 매시 52분 |
| 블로그(창고 A) | `/api/cron-price` | `43 * * * *` | 매시 43분 |
| 블로그(창고 A) | `/api/gsc-sync` | `40 20 * * *` | 한국 05:40 |
| 블로그(창고 A) | `/api/link-guard` | `0 23 * * *` | 한국 08:00 |
| 블로그(창고 A) | `/api/worker-audit` | `47 22 * * *` | 한국 07:47 |
| 샵(창고 C) | `/api/cron/collect` | `*/10 * * * *` | 매시 */10분 |
| 샵(창고 C) | `/api/cron/health` | `0 5,17 * * *` | 한국 14:00,02:00 |
| 샵(창고 C) | `/api/cron/learn` | `32 16 * * *` | 한국 01:32 |
| 샵(창고 C) | `/api/cron/photos` | `50 19 * * *` | 한국 04:50 |
| 샵(창고 C) | `/api/cron/rebuild` | `5,35 * * * *` | 매시 5,35분 |
| 샵(창고 C) | `/api/cron/warm` | `28 */6 * * *` | 한국 09:28,15:28,21:28,03:28 |
| 스튜디오(창고 A) | `/api/cron/booking-health` | `0 0 * * *` | 한국 09:00 |
| 스튜디오(창고 A) | `/api/cron/drive-watch` | `0 2,5,7,12,14,17,21 * * *` | 한국 11:00,14:00,16:00,21:00,23:00,02:00,06:00 |
| 스튜디오(창고 A) | `/api/cron/hotel-addr-fill` | `42 * * * *` | 매시 42분 |
| 스튜디오(창고 A) | `/api/cron/hotel-closed-check` | `0 4 * * 1` | 한국 13:00 |
| 스튜디오(창고 A) | `/api/cron/hotel-district-fill` | `0 3 * * *` | 한국 12:00 |
| 스튜디오(창고 A) | `/api/cron/hotel-fill` | `32 * * * *` | 매시 32분 |
| 스튜디오(창고 A) | `/api/cron/hotel-geo-fill` | `0 16 * * *` | 한국 01:00 |
| 스튜디오(창고 A) | `/api/cron/hotel-geo-fill?mode=district` | `0 8 * * *` | 한국 17:00 |
| 스튜디오(창고 A) | `/api/cron/hotel-geo-fill?mode=district` | `30 12 * * *` | 한국 21:30 |
| 스튜디오(창고 A) | `/api/cron/hotel-geo-fill?mode=district` | `30 16 * * *` | 한국 01:30 |
| 스튜디오(창고 A) | `/api/cron/kw-audit` | `17 5 * * *` | 한국 14:17 |
| 스튜디오(창고 A) | `/api/cron/kw-survey` | `8 * * * *` | 매시 8분 |
| 스튜디오(창고 A) | `/api/cron/push-to-shop` | `25 */3 * * *` | 한국 09:25,12:25,15:25,18:25,21:25,00:25,03:25,06:2 |
| 스튜디오(창고 A) | `/api/cron/screen-sweep` | `20 21 * * *` | 한국 06:20 |
| 스튜디오(창고 A) | `/api/cron/wiring-audit` | `50 5 * * *` | 한국 14:50 |
| 스튜디오(창고 A) | `/api/cron/wiring-check` | `0 1 * * *` | 한국 10:00 |
| 스튜디오(창고 A) | `/api/cron/yt-views` | `2 * * * *` | 매시 2분 |
| 스튜디오(창고 A) | `/api/ops/handoff-verify` | `0 22 * * *` | 한국 07:00 |

### 되돌리는 법

각 저장소 `vercel.json` 커밋 하나씩. 🔴 **되돌릴 수 없는 것: 없다.** 코드는 한 줄도 안 바꿨고 «시각»만 옮겼다.

## 44. 2026-09-10 · 「갤러리 있는 곳만」 보기 — 사진을 담을 수 있는 호텔을 찾는 길

**대표님** — *«갤러리에 사진이 서버에 있어서 선택할 수 있는 호텔을 어떻게 확인할 수 있을까? 지금은 확인할 방법이 없어»*

갤러리 주소가 있는 호텔은 **4,776곳 중 40곳뿐**이라 목록을 넘겨서는 못 찾는다.

### 🔴 고치다 발견한 것

**관리자 호텔 목록은 «손님 화면에 나오는 곳»만 보여준다.** 갤러리 40곳 중 그런 곳은 **7곳뿐**이었다.

| 어디 | 곳 | 갤러리 | 담김 |
|---|---|---|---|
| 소개 | 5 | 800장 | 50 |
| 신상 | 2 | 283장 | 20 |
| 후보 | 11 | 972장 | 70 |
| 🔴 **목록 밖** | **22** | **2,140장** | 220 |

**사진을 담는 일은 «노출 여부»와 상관없다** — 아직 안 올라온 호텔도 담아 두면 올라올 때 바로 쓰인다.
→ 「갤러리 있는 곳만」은 **갤러리 기준으로 40곳 전부** 보여준다.

### 만든 것

| 곳 | 무엇 |
|---|---|
| 창고 `v_shop_gallery_hotels` (신설 뷰) | 갤러리가 있는 호텔 한 줄씩 — 이름·도시·성급·**갤러리 장수**·**담긴 장수**·**「어디」**(소개/신상/후보/목록 밖) |
| 🔴 이름 채우기 | 샵 목록에 없는 22곳은 **아고다 명부(`shop_hotel_master` 301만 곳)** 에서 이름을 채운다. 「(이름 모름)」 **0곳** |
| 창구 `tab=hotels&pool=1` | 갤러리 기준 목록 · 개수 함께 |
| 화면 | **「갤러리 있는 곳만 (40)」** 단추 · 켜면 「가장 싼 날·차액」 대신 **「어디」** 칸이 나온다 |

### 되돌리는 법

`drop view v_shop_gallery_hotels` · `api/admin/index.js`·`public/admin.html` 커밋 각 하나.
🔴 **되돌릴 수 없는 것: 없다.** 손님 화면은 건드리지 않았다.

### 🔴 배운 것

**«관리자 목록에 없다»가 «자료가 없다»는 뜻은 아니다.** 목록의 기준(노출 중인 호텔)과 일의 기준(갤러리가 있는 호텔)이 다르면,
**일의 기준으로 보는 화면을 따로 준다.** 목록에 억지로 끼워 넣지 않는다.

## 45. 2026-09-10 · 🔴 「갤러리 있는 곳만 **(10)**」이 누르면 **40**으로 바뀌던 것

**대표님** — *«갤러리 있는 곳 10곳, 클릭하면 40곳으로 바뀐다. 이런 왜 이래?»*

### 원인 — 「무엇 한 줄인 표인가」를 헷갈렸다

개수를 **`shop_photo_pool` 에서 셌다.** 그런데 그 표는 **«사진 한 장이 한 줄»**(4,195줄)이다.
창구(PostgREST)는 한 번에 **1,000줄**까지만 준다 — **앞 1,000장이 10곳에 몰려 있어** 「10곳」으로 세어졌다.
누른 뒤에는 **«호텔 한 줄»인 `v_shop_gallery_hotels`**(40줄)를 봤으니 40이 나왔다.

### 고침

🔴 **호텔 수는 «호텔 한 줄»인 표에서 센다.** 사진 표에서 세지 않는다. 켜기 전·후 모두 **40**.
안 쓰게 된 `poolFilter` 도 함께 지웠다 — **죽은 코드를 남기지 않는다.**
그리고 갤러리 보기에서는 **갈래 고르개(소개/신상/전체)를 잠갔다** — 갤러리는 갈래를 가리지 않는데 「소개한 호텔」이라 적혀 있어 헷갈렸다.

### 🔴 배운 것 (되풀이 금지)

**「몇 곳인가」를 «장수 표»에서 세지 마라.** 표마다 «한 줄이 무엇인지»가 다르다 —
`shop_photo_pool` 은 사진 한 장, `v_shop_gallery_hotels` 는 호텔 한 곳이다.
그리고 🔴 **창구는 한 번에 1,000줄까지만 준다** — 그보다 많은 표에서 `distinct` 를 세면 **조용히 틀린 답**이 나온다.
셀 때는 «세려는 단위와 같은 표»를 쓰거나 창고 함수로 센다(§56 `shop_admin_counts` 와 같은 방식).

### 45-B · 🔴 같은 날 두 번째 실수 — 「없습니다」가 뜬 이유

§45 를 고치면서 **`poolHotels` 를 «쓰는 곳보다 뒤»에 선언했다.**
자바스크립트에서 `const` 는 «선언 앞»에서 쓰면 그 자리에서 터진다(TDZ) — 창구가 통째로 500 이 되고
화면은 개수가 사라진 채 **「없습니다」** 만 보였다.

🔴 **검사기가 못 잡는 종류다** — 문법은 맞고 «실행할 때» 터진다(§38 과 같은 계열).
🔴 **고칠 때 «변수를 옮기면 선언 위치도 함께 옮긴다».** 지우고 붙이는 순서를 바꿨을 뿐인데 화면이 죽었다.
🔴 **고친 뒤에는 «그 화면을 실제로 눌러» 확인한다.** 창고만 재고 끝내지 않는다 — 창고는 40을 잘 주고 있었다.

## 46. 2026-09-10 · 🔴 사진이 들어오는 «네 길»을 한 규격으로 (새 사진만 무거워지던 것)

**대표님** — *«사진을 몇 시 몇 시에 가지고 오는 거야?»* 그 답을 찾다가 발견했다.

### 🔴 문제 — 전량은 WebP 로 바꿨는데 «앞으로 들어올» 사진은 옛 규격이었다

사진이 우리 창고로 들어오는 길은 **넷**이다.

| 길 | 언제 | 무엇을 | 규격(전) |
|---|---|---|---|
| `api/cron/collect.js` | **10분마다** | 소개 호텔 중 사진이 빈 곳 | 🔴 **2000px JPEG** |
| `api/cron/photos.js` | **한국 04:50** | 신상 후보 중 사진이 빈 곳 | 🔴 **2000px JPEG** |
| `api/admin/upload.js` | 대표님이 올릴 때 | 직접 올린 사진 | 🔴 **2000px JPEG q82** |
| `api/cron/photo-import.js` | 「고른 N장 담기」 | 갤러리 사진 | ✅ 1600px WebP (오늘 만든 것) |

**오늘 4,824곳을 WebP 로 바꿔 놓고도, 내일 들어오는 새 호텔은 다시 JPEG 로 쌓일 뻔했다.**

### 고침 — 넷을 «같은 규격»으로

**상세 1600px WebP q78 · 목록 800px WebP q75** (§40 확정 규격).
🔴 덤으로 **아고다 호출이 줄었다** — 전에는 800·2000 두 장을 «따로 받았는데», 이제 **원본 한 장만 받아 우리가 두 벌을 만든다.**
`photo_spec` 표시도 `800/2000` → **`800w/1600w`** 로 바꿔, 옛 규격인 곳은 다음 차례에 자동으로 새 규격이 된다.

### 되돌리는 법

세 파일 커밋 각 하나. 🔴 **되돌릴 수 없는 것: 없다.** 이미 들어온 사진은 건드리지 않았다.

### 🔴 배운 것

**「전량을 바꿨다」로 끝이 아니다. «앞으로 들어올 것»도 같이 바꿔야 한다.**
규격을 바꿀 때는 **그 자료가 들어오는 길을 «전부» 세어 본다** — 여기서는 넷이었고, 셋이 옛 규격이었다.
🔴 한 곳만 다르면 **그 길로 들어온 것만 무거워져** 나중에 찾기 어렵다.

## 47. 2026-09-10 · 「호텔·사진」 목록에 나라·도시·성급·이름 고르개

**대표님** — *«첫 번째 페이지도 나라도 도시별 성급별 이름 분류하는 메뉴가 필요할 것 같애»*

### 넣은 것

「값 변동」 화면과 **같은 모양·같은 차례**로 두었다 — 화면마다 다르면 매번 다시 배워야 한다.

`갈래(소개/신상/전체)` → `나라 전체` → `도시 전체` → `성급 전체` → `호텔 이름` → `찾기` → `전체 보기` → `갤러리 있는 곳만`

| 곳 | 무엇 |
|---|---|
| 창구 `tab=hotels` | `country`·`star` 를 받는다. 소개·신상·전체·갤러리 **네 갈래 모두**에 걸린다 |
| 창구 응답 | `cities`(v_shop_city 60곳) 를 함께 보낸다 — 화면이 고르개를 그릴 수 있게 |
| 화면 | 🔴 **나라 목록은 도시 목록에서 중복을 지워 만든다** — 나라 표를 따로 두지 않는다(한 곳에서 온다) |
| 도시 고르개 | 나라를 고르기 전에는 **잠긴다**(값 변동 화면과 같다) |

### 되돌리는 법

`api/admin/index.js` · `public/admin.html` 커밋 각 하나. 🔴 **되돌릴 수 없는 것: 없다.**

### 🔴 배운 것 — 배포 확인

커밋 직후 화면을 받아 보니 **옛 것**이었다. 깃허브에는 들어가 있었다 — **Vercel 이 아직 짓는 중**이었다.
🔴 **배포 확인은 «깃허브에 들어갔나»와 «화면에 나왔나»를 나눠서 본다.** 90초로 모자랄 때가 있다 —
깃허브에 있고 화면에 없으면 «실패»가 아니라 «아직»이다. 다시 세어 본다.

### 47-B · 🔴 「HCOUNTRIES is not defined」 — 같은 실수를 하루에 세 번 했다

§47 을 넣으면서 **선언을 «엉뚱한 함수»에 넣었다.**
`const rows = d.rows || [];` 를 찾아 그 뒤에 붙였는데, **그 줄은 화면마다 있다** —
첫 번째로 걸린 것이 「나라·도시」 화면(`viewPlaces`)이었고, 정작 쓰는 「호텔·사진」 화면(`viewHotels`)에는 안 들어갔다.

### 🔴 하루에 세 번 같은 계열의 실수를 했다

| | 무엇 |
|---|---|
| §38 | 부품 «이름»을 바꾸고 쓰는 곳을 다 못 고쳐 「불러오는 중」에서 멈춤 |
| §45-B | 변수를 «쓰는 곳보다 뒤»에 선언해 창구가 터짐 |
| **§47-B** | 선언을 «엉뚱한 함수»에 넣어 화면이 안 열림 |

**공통점 — 전부 「어디에 넣느냐」를 틀렸고, 검사기가 못 잡는 종류다**(문법은 맞고 실행할 때 터진다).

### 🔴 다음부터 지킬 것

1. **흔한 줄을 기준 삼아 붙이지 마라.** `const rows = d.rows || [];` 는 화면마다 있다 —
   붙일 자리는 **함수 이름과 «함께»** 잡는다(예: `async function viewHotels(){` 부터 찾은 뒤 그 안에서).
2. **붙인 뒤 «몇 줄인지»를 센다** — 선언 줄·사용 줄·함수 시작 줄을 뽑아 **「함수 안이고 사용보다 앞인가」를 숫자로 확인**한다.
3. **고친 뒤 그 화면을 실제로 연다.** 창고만 재고 끝내지 않는다(§45-B 와 같은 교훈).

🔴 **안전망(§38 `safeView`)이 값을 했다.** 빈 화면 대신 **「HCOUNTRIES is not defined」** 가 그대로 떠서 대표님이 바로 알려 주실 수 있었고, 원인을 1분 만에 찾았다.

## 48. 2026-09-11 · 🔴 없어진 호텔을 손님 화면에서 뺀다

**대표님** — *«이 호텔은 폐업해서 없어진 거야? 소개한 호텔 중에 폐업했거나 영업이 종료한 호텔을 여기에 적용하면 안 된다. 없어졌으니깐. 빠져야 된다. …아고다 호텔 DB 를 보면 영업하는 곳 안 하는 곳 확인할 수 있는 거 아니야. 이 부분도 다시 정석적으로 체크해서 적용해.»*

### 세 곳을 다 확인했다 (대표님이 짚어 주신 순서 그대로)

| 어디 | 무엇이 있나 | 쓸 수 있나 |
|---|---|---|
| 🔴 **아고다 명부** `shop_hotel_master` | 이름·주소·성급·방수·브랜드… | ❌ **영업 여부 칸이 «없다»** |
| 스튜디오 구글 자료 `hotels.operating_status` | active 3,249 · **closed 3** · temp_closed 1 | ⚠️ **예약이 발생한 호텔 3,253곳만** 다룬다 — 소개 호텔 대부분이 거기 없다 |
| 🔴 **아고다에 직접 묻기** | 있으면 답하고 없으면 «응답에 없다» | ✅ **가장 확실 · 실시간 · 요금 0원** |

### 실측 (2026-09-11)

소개 호텔 **1,075곳** 중 **13곳**이 「값이 한 번도 안 온 곳」이었고,
그 13곳을 아고다에 직접 물었더니 🔴 **13곳 전부 응답이 없었다.** (같이 물은 정상 호텔은 답했다 — 열쇠 문제가 아니다)

대표님이 짚으신 **Super Hotel Premier Osaka Honmachi Ekimae**(H-37912 · 3084987)가 그중 하나였다 —
사진 없음 · 한글 이름 없음 · 값 0건. **아고다에서 사라진 호텔이 맞다.**

### 정한 규칙

```
① 3개월치 주말 값을 하루 여러 번 묻는데 «한 번도» 값이 오지 않는다
② 그 호텔만 따로 아고다에 물어도 «응답에 없다»   ← 확정
→ 없어진 것으로 본다
```

### 넣은 것

| 곳 | 무엇 |
|---|---|
| `shop_hotel.closed_at` · `closed_note` | 없어진 날과 이유. 🔴 **자료를 지우지 않는다 — 되돌릴 수 있다** |
| `v_shop_hotel_silent` (신설 뷰) | 값이 한 번도 안 온 소개 호텔 — 일꾼이 이것부터 본다 |
| 🔴 `api/cron/closed.js` (신설 일꾼) | **한국시간 05:20** · ①조용한 호텔을 아고다에 물어 없으면 표시 ②**없어졌다고 표시한 곳도 다시 물어 되살아나면 되돌린다** |
| 손님 화면에서 빼기 | 🔴 **이미 있는 「숨김」 장치**(`shop_video_hotel.hidden_at`)를 쓴다 — 새 장치를 만들지 않았다. 카드·검색이 한 번에 빠진다 |
| `shop_rebuild_search()` | 신상 쪽에서도 없어진 곳을 뺀다 |

**결과** — 카드에 남은 없어진 호텔 **0곳** · 검색표 **0곳**. 소개 호텔 1,075 → 노출 **1,058**.

### 되돌리는 법

| 것 | 어떻게 |
|---|---|
| 한 호텔만 되살리기 | `update shop_hotel set closed_at=null, closed_note=null where …` + `shop_video_hotel.hidden_at=null` |
| 전부 되살리기 | 위를 조건 없이 · 그 뒤 `select shop_rebuild_search()` |
| 일꾼 끄기 | `vercel.json` 에서 `/api/cron/closed` 한 줄 삭제 |

🔴 **되돌릴 수 없는 것: 없다.** 호텔 자료도 사진도 지우지 않았다.

### 🔴 배운 것

1. **「값이 안 온다」와 「없어졌다」는 다르다.** 만실일 수도 있다 —
   그래서 **그 호텔만 따로 아고다에 물어 «확정»한 뒤**에 뺀다. 짐작으로 빼지 않는다.
2. **되살아나는 것도 본다.** 아고다와 제휴가 끊겼다 다시 붙는 일이 있다 — 한 방향으로만 만들면 영영 안 돌아온다.
3. 🔴 **아고다 열쇠는 «그대로» 넣는다.** `사이트번호:열쇠` 로 만들었다가 401 을 맞았다 — `collect.js` 와 같은 방식을 쓴다.
