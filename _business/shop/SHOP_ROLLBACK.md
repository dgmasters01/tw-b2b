# SHOP_ROLLBACK — 되돌리는 법 (여행능력자들 샵)

> 대표님: *«문제가 생기면 전 단계로 돌아갈 수 있는 기록이 있어야 한다.»*
> 🔴 이 문서는 **바꾼 것마다 «어떻게 되돌리는가»를 그 자리에서 적는다.** 나중에 몰아 적지 않는다.
> 🔴 **되돌릴 수 없는 것**은 «되돌릴 수 없다»고 정직하게 적는다. 그래야 다음에 조심한다.

## 0. 되돌리는 세 가지 수단

| 무엇이 잘못됐나 | 수단 | 방법 |
|---|---|---|
| 코드·문서·설정 | **GitHub 커밋** | 모든 변경이 커밋 1개다. `github-read` 로 옛 커밋의 파일을 받아 다시 올린다 |
| 화면이 죽음 | **Vercel 이전 배포** | Vercel → 프로젝트 → Deployments → 정상이던 배포 → Promote to Production (대표님 화면) |
| 창고 자료 | **표 단위** | 아래 표의 «되돌리는 법» 대로. 표 통째로 지워도 다른 화면에 영향 없게 분리해 두었다 |

---

## 1. 2026-09-05~06 · 신상 호텔 섹션 (D-117)

### 코드·화면 — 전부 커밋으로 되돌아간다

| 파일 | 커밋 | 되돌리면 |
|---|---|---|
| `api/new.js` (신설) | `a938c65` → `f19ef81` | 파일 삭제 시 신상 칸이 사라질 뿐, 다른 화면 영향 0 |
| `public/new.html` (신설) | `52b548d` | 삭제 시 `/new/…` 주소만 사라짐 |
| `public/shop.js` | `942efdb` → `f0752cb` | 신상 부품만 뒤에 붙였다. 그 블록을 지우면 이전과 동일 |
| `public/shop.css` | `0bb8a90` → `6b65a4c` → `ada9b57` | 신상 규격만 뒤에 붙였다 |
| `public/index.html` | `2fdcfea` | `<div id="newbox">` 와 `mountNew(...)` 두 줄만 지우면 원복 |
| `vercel.json` | `e53ab75` → `d662a31` | 주소 1줄 추가 + 크론 주기 변경 |
| `api/cron/collect.js` | `b2c63fd` → `044a912` → `0b827cb` → `d5fcf55` | 🔴 **가장 조심할 파일** — 아래 참조 |

### 창고(Supabase) — 표 단위로 되돌린다

| 표·뷰 | 무엇 | 되돌리는 법 | 다른 곳 영향 |
|---|---|---|---|
| `shop_new_pool` (신설) | 신상 후보 33,659줄 | `drop table shop_new_pool` | **없음** — 신상 칸만 빔 |
| `v_shop_new` (신설) | 화면용 뷰 | `drop view v_shop_new` | **없음** |
| `shop_hotel.photo_src` (칸 추가) | 아고다 원본 사진 주소 | 칸을 두어도 무해. 지우려면 `alter table … drop column` | 없음 |
| `shop_hotel.photo_spec` (칸 추가) | 「800/2000 으로 받음」 표시 | 지우면 **사진을 전부 다시 받게 된다** — 지우지 말 것 | 🔴 있음 |

### 🔴 되돌릴 수 없는 것 (정직하게 적는다)

| 무엇 | 왜 못 되돌리나 | 다시 하려면 |
|---|---|---|
| **사진 파일** | 같은 이름(`thumb.jpg`·`big.jpg`)에 **덮어썼다**. 옛 320px·1,200px 판은 남아 있지 않다 | `photo_spec` 을 비우고 사진 창구를 다시 열어 받는다(약 70분·요금 0원) |
| **가격 기록** `shop_price_daily` | 날짜별로 «그날 잰 값»이라 되돌린다는 개념이 없다. 지우면 「소개 때 가격」 비교가 영영 불가능 | 없음 — **절대 지우지 말 것** |

---

## 2. 2026-09-06 · 수집 봇 개조 — 문제 생기면 여기부터 되돌린다

봇이 잘못되면 **가격 딱지 전체가 죽는다.** 가장 위험한 변경이므로 3단계로 되돌린다.

| 단계 | 증상 | 되돌리는 법 |
|---|---|---|
| ① 아고다가 차단(403·429) | `shop_collect_log.note` 에 `🔴차단` 이 찍힘 | `vercel.json` 크론을 `0 * * * *`(1시간)로 되돌린다 |
| ② 그래도 차단 | 계속 찍힘 | `collect.js` 의 `POOL = 4` 를 `1` 로 (동시 요청 없앰) |
| ③ 그래도 이상 | 값이 안 쌓임 | `collect.js` 를 커밋 `b2c63fd`(신상 편입 전) 으로 되돌린다. 소개 호텔 1,050곳만 수집하던 상태 |

**정상 판정 기준** — `shop_collect_log.note` 가 `cid=1919025 · 코드={"200":N}` 이고 `🔴차단` 이 없으면 정상.

---

## 3. 2026-09-05 · gohotelwinners 배포 사고 (해결됨 · 재발 방지)

| | |
|---|---|
| 증상 | 09-05 15:36 UTC 부터 **모든 배포가 Configuration error**. 빌드 로그 0줄 |
| 원인 | `vercel.json` 의 `ignoreCommand` 를 13:33 에 **제외 15개(428자)** 로 확장 |
| 어떻게 찾았나 | 커밋 시각과 실패 시각 대조 (13:33 확장 → 15:36 첫 실패) |
| 조치 | shop 에서 **실제로 배포되고 있는** 짧은 형태(70자)로 축소 → 커밋 `71ce70e` → 배포 READY 확인 |
| 🔴 재발 방지 | **`ignoreCommand` 를 늘릴 때는 한 번에 하나씩 늘리고, 늘릴 때마다 배포 성공을 눈으로 확인한다.** 이 값은 문법 검사에 안 걸리고 조용히 배포 전체를 막는다 |
| 남은 일 | 빌드 요금 절감이 원래 목적이었다. 제외 폴더를 **하나씩** 다시 늘리며 매번 배포를 확인한다 |

---

## 4. 2026-09-06 오후 · 행동 장부 · 짧은 주소 · 세부 페이지 (D-117)

### 새로 만든 것 — 지워도 손님 화면이 죽지 않는다

| 표·뷰 | 무엇 | 되돌리는 법 | 다른 곳 영향 |
|---|---|---|---|
| `shop_share` `shop_view` `shop_like` `shop_price_alert` | 행동 장부 | `drop table` | **없음** — 기록만 멈춤 |
| `shop_link` | 짧은 주소 | `drop table` | 🔴 **있음** — 이미 뿌린 `gohpik.com/h/{코드}` 가 전부 죽는다. **뿌린 뒤에는 지우지 말 것** |
| `shop_price_stat` | 미리 계산한 가격 요약 | `drop table` 후 재생성 가능(8초) | 신상 카드·세부의 가격이 사라짐 |
| `v_shop_hotel_name` 외 상세 뷰 6종 | 관리자용 | `drop view` | 없음 |

### 코드 — 커밋으로 되돌린다

| 파일 | 무엇 | 되돌리면 |
|---|---|---|
| `api/track.js` (신설) | 열람·공유·찜 기록 | 기록만 멈춤. 화면 영향 0 |
| `api/link.js` `api/s.js` (신설) | 짧은 주소 발급·해석 | 짧은 주소가 죽는다 — 뿌린 뒤에는 주의 |
| `api/h.js` `public/h.html` (신설) | 호텔 세부 페이지 | 카드·줄이 `/h/…` 로 보내므로 **먼저 링크를 `/go` 로 되돌려야 한다** |
| `api/go.js` | 봇 제외 + 방문자·기기·나라·링크코드 | 되돌리면 가짜 클릭이 다시 섞인다 |
| `api/cron/collect.js` | 조식·정가·할인율 저장 | 되돌리면 그 칸만 빔. 가격 수집은 정상 |
| `public/shop.js` | 기록 부품·newRow·세부 링크 | 🔴 **가장 조심할 파일** — 모든 화면이 이걸 쓴다. 되돌리면 화면 전부에 영향 |
| tw-b2b `vercel.json` | `/h/:code` → 샵 (302) | 짧은 주소만 죽음. gohotelwinners 영향 0 |

### 🔴 되돌릴 수 없는 것

| 무엇 | 왜 |
|---|---|
| 행동 기록(열람·공유·찜·클릭) | 그날 있었던 일이다. 지우면 그 손님이 무엇을 했는지 영영 모른다 |
| 이미 뿌린 짧은 주소 | 남의 카톡·설명란에 있다. 우리가 회수할 수 없다 |

### 순서 규칙
1. 화면을 되돌릴 때는 **링크부터** — `/h/…` 를 `/go/…` 로 되돌린 다음 세부 페이지를 지운다. 반대로 하면 손님이 404 를 본다
2. `shop.js` 는 마지막에 되돌린다 — 모든 화면이 여기에 매달려 있다
3. 되돌린 뒤 **반드시 화면 5곳을 직접 열어 200 을 확인한다** (`/` `/japan/tokyo` `/new/japan/tokyo` `/h/411541` `/likes`)
