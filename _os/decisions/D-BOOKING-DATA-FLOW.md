# 결정: 예약 데이터 흐름 + 숫자 갱신 정책 (D-BOOKING-DATA-FLOW)
날짜: 2026-06-07 / 확정: 이지형 대표님

## 배경 (반복 방지 — 이미 2026-04-27 논의됨)
대표님 원문: "아고다 판매데이터는 한 달에 한 번 엑셀파일을 업로드하면 데이터가 자동으로 정리되어야 돼."
→ 이 정책을 박제하지 않아 동일 논의를 반복함. 본 문서로 확정·고정.

## 비즈니스 목적 (기준)
한국(+해외) 여행자에게 호텔을 영상으로 노출 → 실제 예약 발생. $200 1회·6개월 보장·0건 환불.
핵심 지표 = "조회수"가 아니라 **예약(건수·매출)**.

## 데이터 흐름 (확정)
1. 예약 데이터는 **실시간 아님**. 아고다 어필리에이트 리포트를 **매달 1회 엑셀로 수동 업로드**.
2. 업로드 위치 = admin "예약 입력/아고다 매칭" 탭. 적재 테이블 = `bookings_agoda`
   (upload_batch_id, source_filename, uploaded_by 로 배치 관리 — 이미 이 구조로 설계됨).
3. 업로드 시 **자동 집계** → 메인·관리자·매니저 모든 화면 숫자가 자동 갱신.
   **사람이 숫자를 직접 고치지 않는다.**
4. 단일 진실(Single Source of Truth) = `bookings_agoda`. 정적 booking-analytics.html(969건)은 과거 자료 → 추후 1회 이관 대상.

## 숫자 표시 정책
- 실시간 카운터처럼 보이게 하지 않는다(거짓 됨).
- 누적 숫자엔 **기준 시점 명시**: 예) "2026년 5월 기준 누적 3,774건".
- 비용 절감: 매 방문마다 DB 집계 ❌ → 하루 1회(또는 업로드 시) 집계해 저장본 표시.

## 현재 상태 / 약한 고리
- `bookings_agoda` = 0건 (아직 새 시스템에 실데이터 미적재).
- 따라서 "예약 숫자"를 메인에 자동연결하려면 → 먼저 ① 엑셀 업로드→집계 파이프라인 작동 확인 ② 과거 데이터 이관이 선행되어야 함.
- 메인 디자인보다 이 데이터 흐름이 먼저 살아야 숫자가 진짜가 된다.

## 다음 작업 후보
- BL-AGODA-UPLOAD-PIPELINE: admin 엑셀 업로드 → bookings_agoda 자동 집계 검증/완성.
- BL-LEGACY-BOOKING-MIGRATE: booking-analytics 969건 → bookings_agoda 이관.
- 그 후 메인/대시보드 숫자를 집계값에 자동 연결(+기준시점 표시).
