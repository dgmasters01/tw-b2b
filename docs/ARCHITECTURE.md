# 🏛 시스템 구조 정본 (ARCHITECTURE)

> **최종 갱신** 2026-08-11 · 실측 기반
> **이 문서의 목적**: 문제가 생겼을 때 «다른 방향으로 가지 않도록» 구조와 이유를 한 곳에 박아둔다.
> 🔴 **구조에 관한 판단은 반드시 이 문서를 먼저 읽고 한다.** (대표님 지시 2026-08-11)
> **그림**: `docs/system-map.svg`

---

## 0. 한 줄 원칙

> ### 명부는 한 권 같이 · 장부는 각자 · 문도 각자

| 무엇 | 어떻게 | 왜 |
|---|---|---|
| **명부**(아고다 호텔 자료) | **한 벌**. 모두 «읽기만» | 아고다가 만든 것. 잃어도 6분이면 다시 받음. 나누면 「같은 호텔인가」 대조가 깨짐 |
| **장부**(우리가 만든 것) | **사업마다 따로** | 예약·매출·소개이력·구독자. 잃으면 영영 못 되돌림 |
| **문**(창구 API) | **사업마다 따로** | 한쪽이 죽어도 다른 쪽이 살아야 함 |

🔴 **나누는 건 「문」이지 「창고」가 아니다.** (대표님이 헷갈리셨던 지점 · 2026-08-11)

---

## 1. 4층 구조

```
[관리실]  개인업무시스템 1hogi.gohotelwinners.com  ← 최상위. 보기만 함
    │ 묻기만 (아무것도 안 고침)
    ├──────────────┬──────────────┬─────────────┐
[① 스튜디오]   [② 블로그]    [③ 패키지여행]   [④ 다음 사업]
gohotelwinners  staycurate     (예정)
    │              │
[문①]          [문②]        각자 문 신설
90회/시간      90회/시간
    └──────┬───────┘
   [공동 명부 hotel_master]  3,015,718곳 · 읽기만
```

### 🔴 관리실을 «사업 안»에 두지 않는 이유
소방서를 건물 안에 지으면 불났을 때 같이 탄다.
스튜디오 안에 점검판을 두면 → 스튜디오가 죽었을 때 «죽었다고 알려줄 화면»도 죽는다.
→ 점검은 **GitHub Actions**(우리 서버 밖)가 매시간 밖에서 두드린다. 결과는 `_os/status.json`.

---

## 2. 지금 상태 (2026-08-11 실측)

| 항목 | 값 |
|---|---|
| 호텔 명부 | **3,015,718곳 · 221개국 · 73,791도시** |
| 스튜디오 호텔 | 3,253곳 (아고다 번호 연결 **2,955**) |
| 블로그↔스튜디오 대조 가능 | **192곳** |
| 블로그 자격 통과(8.0+·400+) | 34,915곳 |
| 블로그 후보 도시(7곳+) | 615개 |
| 표 잠금 | **77 / 77** ✅ |
| 창고 용량 | 2.6 GB / 8 GB |
| 비용 | 월 약 $45 (Supabase Pro $25 + Vercel Pro $20) |

---

## 3. 창구(문) 정본

| 문 | 주소 | 몫 | 열쇠 |
|---|---|---|---|
| 스튜디오 | `gohotelwinners.com/api/ops/*` | 90회/시간 | tw-b2b 환경변수 |
| **블로그** | `www.staycurate.com/api/ops/db-query` | 90회/시간 | staycurate 전용 `staycurate-blog-door` |
| 문 상태 확인 | `www.staycurate.com/api/ops/doorcheck` | — | 인증 불필요 |

- 전체 상한 **120회/시간** = DB 보호선. 🔴 **절대 올리지 않는다** (2026-07-22 에 600 으로 올렸다가 DB가 뻗어 로그인까지 멈춤).
- 스튜디오 문 호출 시 헤더 `x-ops-client: studio | blog | package | personal` (없으면 studio).
- 블로그 문은 **스튜디오 장부 쓰기 차단**(`hotels`·`bookings_*`·`publications`·`admins` 등). 읽기는 자유.
  - 🔴 검사 대상은 **쓰기 «대상 표 이름»만**. 예전에 SQL 전체를 훑어 «호텔 이름에 hotels 가 든 자료»까지 막던 버그가 있었다(2026-08-11 수정).
- 블로그 열쇠 만료 시 **자동으로 스튜디오 문으로 우회**(`door: upstream`). 서비스 정지가 아니라 «차선 합쳐짐».
- 🔴 **블로그 열쇠 만료 2027-08-10.** 점검판에 D-day 표시. 만료 전 재발급.

### 아직 스튜디오 쪽에 있는 창구 (블로그도 이걸 씀)
`github-commit` · `github-read` · `blog-stats` · `pool-weekends`
→ 이 넷은 gohotelwinners 가 죽으면 블로그도 못 쓴다. **다음 분리 과제.**

---

## 4. 새 사업을 시작할 때 (표준 6단계)

앞으로 사업이 늘어도 **이 6개만** 만든다. 매번 구조를 다시 논의하지 않는다.

1. 자기 도메인 (Cloudflare Registrar)
2. 자기 GitHub 레포
3. 자기 Vercel 프로젝트
4. **자기 문 + 자기 열쇠 + 자기 몫(90회)** — `staycurate/api/ops/db-query.js` 복사해서 이름만 바꿈
5. **자기 이름표** (`blog_` 처럼 `ceylon_` · `package_`)
6. 관리실 `_os/status-targets.json` 에 **한 줄 추가** → 신호등 자동 생성

### 창고를 «같이» 쓸지 판단하는 기준 한 줄
> **「한 줄 한 줄을 맞대볼 일이 있나?」**
> 있다 → 같은 창고 (스튜디오↔블로그. 같은 호텔인지 대조해야 함)
> 없다 → **따로.** 문으로 물어보면 됨 (패키지여행·CEYLON 등 대부분)

### 조직(Organization) 은 하나로 모은다
🔴 **조직 분리는 보안 방어선이 아니다.** 로그인 계정이 하나면 조직을 나눠도 같이 털린다.
조직은 «청구서 단위»일 뿐 → 새 사업은 **같은 조직에 새 프로젝트**로. (조직 2개면 기본료 $25 를 두 번 냄)
**프로젝트끼리는 같은 조직 안에서도 완전히 분리된다.** 격리는 프로젝트가 만든다.

---

### 4-B. 🔴 사업이 늘어날 때의 한도 설계 (2026-08-16 · D-094)

대표님 지적: *「지금처럼 푸시하고 자료 검색하고 개발하면 여기도 한도가 걸리잖아. 앞으로 여러 개의 사이트·블로그를 만들어야 된다. 하나만 생각하지 마라.」* 맞다. §4 의 6단계는 **사업을 만드는 법**만 있고 **사업이 늘 때 한도가 어떻게 되는지**가 없었다.

#### 숨어 있던 함정 — 몫은 늘어도 천장은 안 늘어난다
사업마다 문에 90회/시간을 준다. 그런데 **전체 상한 120회/시간은 그대로다**(DB 보호선·절대 불변).
→ 사업 2개 = 180 요구 vs 120 천장 / 사업 4개 = 360 요구 vs 120 천장.
**사업이 늘수록 각자 몫이 줄어든다.** 90회는 «약속»이 아니라 «상한»일 뿐이다.

#### 그래서 원칙을 하나 더 세운다
> ### 사업이 늘어도 «문 두드리는 횟수»는 늘지 않게 만든다

| 늘려도 되는 것 (선형 증가 OK) | 절대 늘리면 안 되는 것 |
|---|---|
| 도메인 · GitHub 레포 · Vercel 프로젝트 | **DB 인스턴스** (하나) |
| 이름표 prefix (`blog_`·`shop_`·`ceylon_`) | **봇 종류** — 한 봇이 사업들을 순회한다 |
| 상태 신호등 한 줄 | **외부 API 계정** (아고다·구글) |
| 문(코드는 복사본 1개, 이름만 다름) | **규칙 문서** — 사업마다 새 헌법 금지 |

#### 병목 5개와 처방 (성격별)

| 병목 | 한도 | 성격 | 처방 |
|---|---|:---:|---|
| DB 창구 | **120회/시간 (천장)** | 우리가 건 것 | 🔴 올리지 않는다. **화면이 DB를 안 묻게** 한다 — 새벽에 JSON 으로 구워 CDN 에 올리고 화면은 파일을 읽는다. 사업 10개여도 DB 호출 0 증가 |
| 저장 창구 `github-commit` | 30회/시간 | 우리가 건 것 | **묶음 커밋**(한 번에 파일 N개) · 초안·시안은 커밋하지 않고 확정만 · 배포 확인 폴링 줄이기. 2026-08-16 시안 기획 한 건에 8회를 썼다 = 사이트 4개 동시 개발이면 즉시 막힘 |
| 아고다·구글 API | 90회/시간 | 남이 건 것 | **창고 1개 + 수집 봇 1개.** 새 사이트는 창고에서 꺼내 쓰기만 → 사업이 늘어도 외부 호출 **0 증가** |
| GitHub Actions 분 | 월 한도 | 남이 건 것 | 봇을 사업마다 복제하지 않는다. **한 봇이 사업 목록을 돌며** 처리 |
| Vercel · Supabase 요금 | 돈 | 돈으로 푸는 것 | 지금 병목 아님(월 $45). 매출 나면 즉시 해결. **여기서 아끼려다 위 4개를 망가뜨리지 않는다** |

#### 🔴 착각 방지 — 문 한도는 «서비스» 한도가 아니다
120회/시간은 **관리·개발용 창구**다. 손님이 보는 화면은 이 문을 쓰지 않는다(정적 파일 또는 Supabase 직결+RLS).
「손님이 늘면 120에 걸리나?」 → **아니다.** 걸리는 건 우리(클로드)의 개발 작업이다.

#### 새 사이트를 «공장»처럼 찍어낸다
§4 6단계를 매번 손으로 하지 않는다. **템플릿 레포 1개**(문·인증·창고 연결·신호등 등록이 이미 든 것)를 복제 → 이름·도메인·CID만 바꾼다. 개발 시간도 한도 소모도 선형으로 늘지 않게 하는 것이 목표.

#### 측정이 없으면 관리가 안 된다
점검판(ops.html)에 **「이번 시간 남은 횟수 / 오늘 사용률」**을 창구별로 띄운다. 80% 넘으면 경고.
지금은 얼마나 썼는지 볼 화면이 없어서 «막히고 나서» 안다.

---

## 5. 보안 방어선 5겹

| 겹 | 뚫리면 | 막는 법 | 상태 |
|---|---|---|---|
| 1 | Supabase 로그인 계정 | **2단계 인증(MFA)** | 🔴 **폰 교체 후** (대표님) |
| 2 | `ops` 토큰 (임의 SQL 실행) | 열쇠 분리·권한 축소 | ⏳ 개발 완료 후 (대표님 지시) |
| 3 | 프로젝트 자격증명 | 사업별 프로젝트 분리 | ✅ |
| 4 | 표 잠금(RLS) | 표마다 잠금 | ✅ **77/77** |
| 5 | 코드·배포 | 레포·Vercel 분리 | ✅ |

🔴 **표를 만들면 같은 작업에서 잠근다.** 8/4 에 27개를 잠갔는데 3주 만에 24개가 다시 열려 있었다.
→ 사람이 기억하는 규칙은 반드시 샌다. **점검판에 「안 잠긴 표 N개」 상시 표시.**
```sql
alter table public.새표 enable row level security;
```
- 2단계 인증은 **폰 앱(Authenticator)보다 비밀번호 앱(1Password 등)** 권장. 폰을 바꿔도 따라온다.
- 복구 코드는 반드시 따로 보관. 🔴 Claude 에게 보내지 않는다.

---

## 6. 대량 적재 안전 규칙 (9시간 무중단 실전 검증)

🔴 2026-07-22 에 대량 적재로 DB가 뻗어 **로그인까지 멈춘** 이력이 있다. 원인은 «파일 크기»가 아니라 **몰아친 속도**였다.

```
① 새 표에 넣는다              지금 쓰는 표는 손도 안 댐 → 서비스 영향 0
② 매 묶음마다 사이트 응답 측정
      4초 초과 → 쉬고 재확인 → 회복되면 계속
      두 번 연속 이상 → 정지
③ 두 문을 번갈아 쓴다          한도 걸리면 상대 문으로
④ 검증 통과 후 이름 바꿔치기(1초)
⑤ 옛 표는 _old_YYYYMMDD 로 보관
```

### 실전에서 배운 것 (다음에 반드시 참고)
- **묶음이 클수록 서버가 편하다.** 4,000행 배치일 때 응답 0.07초. 문을 자주 여닫는 게 부담.
- 단, **긴 소개글이 몰린 구간**은 500~800행으로 줄여야 통과.
- 🔴 503/502 로 **8개 구간이 통째로 빠졌다.** 총량만 봐서는 못 잡는다 → **id 대조로 구멍을 찾아야 한다.**
- 자료에 **널 문자(\x00)** 가 섞이면 삽입 실패. 걷어낸다.

---

## 7. 호텔 번호 체계 (hotel_code)

```
[명부층]  hotel_master   H-##### 부여. 세상의 호텔 대장. 읽기만
              │ 각 사업이 «번호만» 참조
   [스튜디오]      [블로그]        [패키지여행]
    hotels        blog_*          ceylon_*
   영업 대상       소개 이력        상품 포함 호텔
```

- 🔴 **번호는 절대 바뀌지 않는다**(영구 식별자 · `HOTEL_MATCH.md` 원칙③).
  명부를 43배로 교체할 때도 기존 69,785곳은 **원래 번호를 그대로 승계**했다.
- 신규 발급은 마지막 번호 다음부터. 2026-08-11 기준 신규는 `H-0070646~`.
- 🔴 **블로그가 소개했다고 스튜디오 영업 명단(`hotels`)에 넣지 않는다.** 두 사업은 별개다.
  (대표님 2026-08-11: *"블로그하고 $200 호텔 사업을 연결해서 해석하지 마라"*)

---

## 8. 아고다 명부 갱신

| 무엇 | 주기 | 왜 |
|---|---|---|
| 전체 갈아끼우기 | **분기 1회** | 신규·폐업이 쌓여야 의미 있음 |
| 폐업 확인 | 매달 | 추천 중인 호텔만 확인 (가벼움) |

**실측(10일 기준, 일본)**: 신규 3.8% · 폐업 1.1% · 후기 변동 11%.
단, 자격선(후기 400) 이 뒤바뀐 곳은 0곳 → **평점 때문에 급할 일은 아니다. 진짜 이유는 신규·폐업.**

- 파일 주소는 `agoda_file_source` 표에 보관. 🔴 **확장자는 `.zip`** (`.z` 로 잘못 저장돼 404 로 오인한 적 있음 · 2026-08-11 정정).
- 파일은 425MB(압축) / 1.9GB(CSV) / 3,015,718행 / 39칸.
- 아고다가 주소를 바꿀 수 있다 → 봇이 주소를 눌러보고 죽어 있으면 **점검판 빨간불**. 대표님은 그때만 5분.
- 🔴 **조건을 걸어 일부만 넣지 않는다.** 8/1(15개국)·8/9(6.9만)처럼 조건을 걸면 반드시 부족해져서 또 넣게 된다.

---

## 9. 관리실 점검판

**주소** `https://gohotelwinners.com/ops.html` (임시 위치. 개인업무시스템 완성 후 이전)

| 칸 | 내용 |
|---|---|
| 사업 신호등 | 스튜디오·블로그·개인업무 (GitHub Actions 가 매시간 밖에서 측정) |
| 창고·자료 | 용량 · 명부 · 연결 · 대조 가능 |
| 문·열쇠 | 문 2개 상태 · **블로그 열쇠 만료 D-day** |
| 보안 잠금 | **안 잠긴 표 N개** (0 아니면 빨간불) · 2단계 인증 |
| 비용 | 월 $45 |

관련 파일: `.github/workflows/ops-status.yml` · `_os/status-targets.json` · `_os/status.json`
🔴 사업이 늘면 `status-targets.json` 에 **한 줄만** 추가한다.

---

## 10. 오늘(2026-08-11) 결정·완료 목록

| # | 무엇 | 근거·이유 |
|---|---|---|
| 1 | 아고다 번호 **949곳** 연결 (2,006→2,955) | D-071 잣대 순서 복원 |
| 2 | 잣대 순서 = **예약번호 → 이름 → 거리** | 🔴 한 건물에 여러 호텔. 거리로 고르면 옆 건물을 붙임 |
| 3 | 리뷰수·주소 물증은 **실측 0건** | 우리 값 ≠ 아고다 최신값 / 주소 표기 체계 다름. **작동한 건 이름 완전일치뿐** |
| 4 | 표 **77개 전부 잠금** | 8/4 사고 재발 방지 |
| 5 | 창구 **차선 분리**(사업별 90회) | 한쪽이 독식해 상대가 멈추던 문제 |
| 6 | **블로그 전용 문** 신설 | gohotelwinners 가 죽어도 블로그 발행 계속 |
| 7 | 명부에 **hotel_code** 부여 | 호텔 하나로 세 사업 조회 |
| 8 | 아고다 명부 **301만곳** 구축 (누락 0) | 반복 요청 종결 |
| 9 | 관리실 **점검판** 신설 | 낡은 문서 대신 살아있는 숫자 |
| 10 | `boot.md` 블로그 진입점 정정 | 🔴 404 문서(`INDEX.md`)를 가리켜 3주간 헤맴 |
| 11 | 창고 키우기(B안) **취소** | 블로그 자격이 후기 400+ 라 효과 없음 |
| 12 | 프로젝트 분리($10) **안 함** | 명부 공유가 깨져 대조 불가 |

---

## 11. 남은 과제

| 우선 | 무엇 | 누가 |
|---|---|---|
| 🔴 | **2단계 인증** (비밀번호 앱 권장) | 폰 교체 후 · 대표님 |
| 🔴 | `tw-b2b` 레포 **비공개 전환** (열쇠 노출은 없음 확인) | 대표님 5분 |
| ⚠️ | `manual_pending` 47곳 중 **23곳은 예약번호 물증 있음** | 대표님 판단 |
| ⚠️ | 기존 **중복 23쌍** (같은 아고다번호 2곳) | D-071 병합 |
| | 미연결 298곳 — 이름 불일치로 보류. **억지로 안 채움** | — |
| 🔴 | **묶음 커밋 창구**(한 번에 파일 N개) — 30회/시간 병목 1순위 | 다음 |
| 🔴 | **정적 굽기**(JSON→CDN) 로 화면의 DB 호출 제거 | 다음 |
| ⚠️ | 창구별 **사용률 점검판** 표시 | 다음 |
| ⚠️ | **새 사이트 템플릿 레포** | 다음 |
| | `github-commit` 등 창구 4개 블로그 분리 | 다음 |
| | 분기 자동 갱신 봇 | 다음 |
| | 개인업무시스템 Pro 이전(+$10) · 점검판 이전 | 대표님 판단 |

---

## 12. 되돌리기

```sql
-- 명부
alter table hotel_master rename to hotel_master_x;
alter table hotel_master_old_20260811 rename to hotel_master;

-- 아고다 번호 연결 (전체 / 나라별 / 한 곳)
update hotels set agoda_hotel_id=null, agoda_match_status=null
where agoda_match_status in ('booking_id','auto_geo','auto_name','no_agoda');
--  + and country='Japan'   /   + and hotel_code='H-00561'
```
백업표 `hotels_agoda_backup_20260811` (작업 전 1,247곳).
🔴 기존 2,006곳·`auto_matched`·`manual_pending` 은 조건에서 빠져 **손댈 수 없는 구조**.

---

## 13. 관련 문서

| 무엇 | 어디 |
|---|---|
| 아고다 번호 연결 | `_business/decisions/2026-08-11-agoda-id-link.md` |
| 명부 301만곳 구축 | `_business/decisions/2026-08-11-agoda-master-full.md` |
| 창구 차선 분리 | `_business/decisions/2026-08-11-ops-lanes.md` |
| DB 잠금 2차 | `_business/decisions/2026-08-11-db-lock-round2.md` |
| 블로그 문 사용법 | `staycurate/docs/DOOR.md` |
| 데이터 경계 | `staycurate/docs/DATA-BOUNDARY.md` |
| 호텔 매칭 원칙 | `_business/HOTEL_MATCH.md` (D-071) |
| 시스템 전체 지도 | `SYSTEM_MAP.md` |
