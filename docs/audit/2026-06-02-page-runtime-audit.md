# 페이지 런타임 점검 — 2026-06-02

> **대표님 지시:** "오류나고 페이지 정상 안 되는 곳 다 확인해서 찾아. (예: 호텔파트너 → 매니저 허브 → 로그인하니 빈 화면) 만들다 스톱한 거 정리. 시스템 완성도에 기록 다 하고 있나?"
> **이 문서 = 그 점검 결과 + 기록 체계 진단.**

---

## 한 줄 요약

클릭하면 깨지는 곳을 코드로 전수 추적한 결과, **즉시 차단 1건(매니저 허브 빈 화면)을 찾아 고쳤고**, 만들다 만 페이지 2건을 목록화했습니다. 그리고 대표님 질문대로 — **기록 체계는 "절반만" 돌고 있었습니다**(정적 미완성은 추적, 런타임 깨짐은 사각지대).

---

## 1. 점검 방법

- 전체 운영 페이지(백업·목업·스냅샷 제외) 대상
- (a) 페이지→페이지 링크가 실제 존재하는 파일인지(dead link)
- (b) 로그인 세션 저장소(storageKey) 불일치 — "로그인했는데 빈 화면" 유형의 근원
- (c) 미완성 마커(TODO/준비중/아직 없음)
- (d) 호출하는 서버 기능(action)이 실제 존재하는지

---

## 2. 발견 결과

### 🔴 P0 — 매니저 허브 로그인 루프 (수정 완료)

- **증상:** admin → 호텔 파트너 → "매니저 허브" → 로그인 → **빈 화면 / 무한 리다이렉트**
- **근본 원인:** `admin-manager-hub.html`이 자체 Supabase 클라이언트를 만들면서 **로그인 세션 서랍(storageKey)을 지정 안 함**(기본값 사용). admin 로그인은 `tw-admin-auth` 서랍에 세션을 저장하는데, 허브는 다른 서랍을 봐서 세션을 못 찾음 → 계속 로그인으로 튕김.
- **맥락:** D-050에서 매니저 화면 진입을 이 페이지로 "통일"했으나, **인증 서랍을 안 맞춘 채 멈춘** 상태였음(전형적 "만들다 만 것").
- **수정:** admin-login.html과 동일하게 `storageKey:'tw-admin-auth'` + adminStorage 하이브리드(D-051 로그인 유지 정책) 적용. 검증된 코드 1:1 복제.
- **상태:** ✅ 코드 수정·라이브 반영. **단, 실제 로그인 후 정상 표시는 대표님이 브라우저에서 1회 확인 필요**(인증된 세션 클릭은 사람만 가능).

### 🟡 정적 미완성 페이지 (목록화 — 후속 정리 대상)

- `hotel-info.html` — TODO 14건 + 빈 태그 14건 (admin-status가 이미 표시 중)
- `admin-tasks.html` — TODO/준비중 5건

### 🟢 이상 없음

- **깨진 링크: 0건** — 페이지 간 라우팅 자체는 멀쩡(링크 대상 파일 전부 존재).
- 서버 기능(action) 호출은 여러 라우터에 분산되어 있어 단순 누락으로 보긴 어려움(별도 정밀 점검 권장).

---

## 3. 대표님 질문 직답 — "기록 다 하고 있나?"

**절반만.**

- ✅ **정적 미완성(TODO/빈 태그)은 추적 중.** `admin-status.html`(작업 지휘소)이 페이지별 TODO 개수와 "미구현 사업합의" 배지(부칙20)를 표시함.
- ❌ **런타임 깨짐(로그인 루프·빈 화면·세션 불일치)은 추적 체계 없음.** TODO 글자가 없으니 정적 점검에 안 걸림. 그래서 **대표님이 직접 클릭하다 발견**해야 했음. ← 이번 매니저 허브가 바로 그 사각지대.

---

## 4. 권고 (후속 BL 후보)

1. **BL-FIX-MANAGER-HUB-AUTH** — ✅ 이번 완료.
2. **BL-PAGE-SMOKE-TEST** (신설 권장) — Playwright로 주요 페이지를 "실제 로그인 → 클릭 → 빈 화면/콘솔에러 감지"까지 자동 점검하는 봇. 런타임 사각지대를 메움. health-check에 통합.
3. **hotel-info.html / admin-tasks.html TODO 정리** — 정적 미완성 마감.

---

**점검:** 클로드 (2026-06-02) · 호칭: 대표님 · 언어: 한국어
