> ⚑ **현재 기준 = 아래 「확정 v3 (2026-07-13) — 성과 확인 중심」.** 이 위의 A~F(v2)는 "앞으로 뭘 만들지 고르기(재사용·키워드·전략·놓친것)" 맥락으로, 성과 목록과는 다른 화면이라 보류·참고용으로 남겨둠(삭제 안 함). 목록 화면 자체는 v3를 따른다.

# 스튜디오 · 호텔 메뉴 — 재설계 스펙 v2 (대표님 확정 흐름)

> BL: `BL-STUDIO-MENU-6TAB` / 메뉴 3/6: 호텔 (재설계)
> 기준 = 2026-07-13 대표님과 이미지로 하나씩 맞춘 최종안 (이 문서가 단일 기준, 이미지 아님)
> 앞 문서 D-062(호텔-spec.md)의 "상세 대시보드"는 유지, 그 위에 목적·목록·현황표·연계를 재정의
> 상태: **pending_review → 라이브 반영하며 확정**
> 정리: 클로드 · 2026-07-13

## 목적 (2겹)
1. 스튜디오 호텔 = "이 호텔을 콘텐츠에 어떻게(다시) 쓸지" 전략 판단 대시보드. 취소·확정률·리드타임이 핵심 축.
2. 그 위 사업 목적 = 콘텐츠 보고 소비자가 예약하게 만드는 것.
→ 그래서 "재사용 가능 + 키워드 수요 + 전략 방향 + 성과(돈·전환)"를 묶어 "지금 이 호텔 넣을지" 판단.

---

## A. 상단 현황표 — 2축 (핵심: 시점과 작업을 분리)

### 축 1 · 재사용 시점 (자동으로 바뀜 — 마지막 노출 +6개월)
카드 4: 지금 쓸 수 있음 / 곧 풀림(30일) / 아직 못 씀 / 노출 안 함

### 축 2 · 작업 상태 (사람이 움직임 — 큐 넣기·발행 시 즉시)
카드 4: 안 건드림 / 큐에 넣음(누가·언제 표시) / 발행됨 / **놓친 것**(피크 지남·되살리기 가능, 빨간 점선)

### 좋은 호텔·커버리지 (보조)
평균 확정률 / 확정률 높음(≥85) / 낮음(<50) / 전체 / 나라·도시 / 호텔·리조트

**업데이트 기준**
- 재사용 시점 = 자동(6개월 경과 시 "지금 가능"으로)
- 작업 상태 = 행동 시(큐에 넣으면·발행하면 즉시)
- 놓친 것 = 피크 월 지나면 자동 만료 → "놓친 것"으로 이동 (판정 기준: 피크 월 경과. 대안=추천 후 N일 미실행 — 확정 필요)

핵심 원칙: **골라서 큐에 넣어도 "지금 쓸 수 있음"에 남음**(재사용은 여전히 가능). 대신 `큐에 넣음` 배지로 안 건드린 것과 구분. 사라지지 않음.

---

## B. 목록 + 필터
- 검색: 호텔명·hid (검색창은 필터 드롭다운 **뒤/오른쪽**)
- 필터 드롭다운: 나라 · 도시 · 성급 · **유형(호텔/리조트)**
- 각 행: 호텔(나라·도시·성급 뱃지, 리조트=파란) · 키워드 수요·포지션 · 전략 방향·큐 상태 · 확정률
- 정렬: 추천순(수요×전략) 기본 / 키워드 수요순 / 확정률순 / 나라·도시
- "지금 쓸 수 있음 N개" 카드 열면 → 이 목록(추천순)

---

## C. 행 펼침 (인라인 아코디언, 배경=회색 중립·왼쪽 회색선, 파란색 금지)
행 누르면 그 자리에서 펼침(다른 행 자동 접힘). 담긴 것 위→아래:

### ① 키워드 근거 (D-065 연계 · 조사일 표시)
- 검색량(월) · 3개월 추세(±%↑↓) · 우리 순위
- 검색량 흐름 + 예측 피크 그래프(파랑=실측, 주황=예측)
- 연관 키워드 N개 · 합계 검색량

### ② 전략 근거 (D-066 연계 · 4단 상세 — 비약 금지)
방향 배지 + 근거 4: 1)수요 2)타이밍(제작 6~8주, 지금 만들면 피크 노출) 3)경쟁(순위·여지) 4)호텔 적합. + 결론 한 줄.

### ③ 이 호텔 성과 (돈·전환·노출)
- 돈: 예약금액(모두) · **수수료(대표님만 — 점선+잠금, 에디터·매니저 화면엔 칸 자체 숨김·권한 분기)** · 확정률
- 전환 흐름 = **칸(카드)로**: 조회 › 클릭 › 예약 › 확정, 칸 사이 전환율(%). 확정 칸 초록. (조회·클릭=연동 후, 예약·확정·금액=실데이터)
- 노출(N회·TOP순위) · 리드타임 확정(가장 강한 구간)

### 행동 버튼 2개
- **이 방향으로 콘텐츠 만들기 ↗** = 실행 → 전략 큐(D-066)에 카드 생성. 근거 같이 붙음. 누른 뒤 그 행 = `✓ 큐에 넣음 (누가·날짜)` 배지, 사라지지 않음. 또 누르면 "이미 큐에 있음" 중복 경고(큐에서 보기/그래도 추가). (올리기 맡음·중복 방지 원리)
- **이 호텔 전체 상세 ›** = 더 보기 → **방식 A(같은 창 화면 전환 + `‹ 목록으로` 뒤로가기)**. 상세=D-062 대시보드(노출이력 전체·개별예약 전체·리드타임·채널별·재사용·수수료). (인라인 B·사이드 C 아님 — 모바일·가로넘침금지 D-060)

---

## D. 놓친 것 (지난 추천)
- 피크 지나 자동 만료된 호텔. **숨기지 않음 — 누구나 열람.**
- 클릭하면 → **C의 행 펼침을 그대로 재사용** + 맨 위 "왜 놓쳤나" 박스.
- **기존 데이터 전부 그대로 표시**(검색량·순위·과거 노출·예약·확정·금액·전환 — 비우지 않음). 놓친 이유·다음 피크만 추가.
- 버튼: **되살리기(= 이 방향으로 콘텐츠 만들기 → 전략 페이지로 이동)** · 다음 피크까지 보류 · 전체 상세.

---

## E. 라이브 반영 순서 (실데이터 되는 것부터)
1. 현황표 2축(재사용 시점=실데이터 / 작업 상태=큐·발행 연동 후) + 좋은 호텔·커버리지
2. 목록(재사용 상태·나라·도시·성급·유형 필터·검색 뒤로) — 유형/일부 지역은 마스터 보완 필요
3. 행 펼침 성과·전환(예약·확정·금액=실데이터 / 조회·클릭=연동 후)
4. 키워드·전략·큐·놓친것 연계 = 키워드(D-065)·전략(D-066) 메뉴 실데이터 붙은 뒤 채움 (지금은 "연동 후" 자리만)
5. 전체 상세(방식 A 화면 전환) = D-062 대시보드 연결

---

## F. 확정 필요 (라이브에서 보며 정할 것)
1. "놓친 것" 판정 = 피크 월 경과 vs 추천 후 N일 미실행
2. 큐 중복 시 "그래도 추가" 허용 범위(채널·언어별 몇 개까지)
3. 리드타임 구간 3 vs 6 (앞 D-062 미결과 동일)


---

# 확정 v3 (2026-07-13) — 성과 확인 중심 (현재 라이브 기준)

## 목적 정정
스튜디오 호텔 목록 = **우리 콘텐츠에 노출한 호텔들의 성과를 확인·정리하는 곳**.
"지금 쓸 수 있음·안 건드림·큐에 넣음" 같은 재사용/작업 상태와 키워드·전략 연계는 **여기가 아님**(앞으로 뭘 만들지 고르는 다른 맥락 → 나중에 전략/추천 화면으로). 목록엔 넣지 않는다.

## 1) 상단 현황표 — 성과·커버리지만
카드 6 (순서 고정): **전체 · 나라·도시 · 호텔·리조트 · 평균 확정률 · 높음 ≥85 · 낮음 <50**
- 규모(전체·나라도시·호텔리조트) 먼저 → 성과(평균·높음·낮음) 나중.
- **카드 = 필터**(펼침 아님). 카드 누르면 **아래 목록 하나**가 그 조건으로 걸러짐 + 선택 카드 강조 + "전체 보기 ✕". 목록은 화면에 하나만.
- 지금 클릭: 전체·높음·낮음(+노출안함). **나라·도시 / 호텔·리조트 = 누르면 분포(개수순) 목록, 하나 고르면 걸러짐 — 다음 작업(팝오버 UI).**
- (재사용 시점·작업 상태 두 축은 v3에서 목록 화면에선 뺌.)

## 2) 대량 대응 (수만 개) — 글로벌 표준
- **필터가 1차**(나라·성급·검색으로 수백 개↓로 압축) → **페이지 번호**로 넘김(‹ 1 2 3 … ›) → **서버가 그 페이지만**(추후 서버 페이징, 지금은 클라이언트).
- 페이지당 25/50/100 선택(기본 50). 정렬: 추천순(임시=노출×확정률)·확정률순·노출순.
- 목록 위 항상 "전체 N → 결과 M개 · 1–50 표시".

## 3) 목록 한 줄 = 종합 (호텔 하나 전체 성적)
- 정체: **한글명 · 나라·도시·성급·유형 · 영문명(작게, 검색용 — 영문 검색 대응, 표시는 유지)**
- 성과 = **[소개] → [결과] 두 묶음**:
  - 소개: 대표 채널명(외 N) · **최고 TOP**(여러 노출 중 최고 순위) · **노출 N회**(합산)
  - 결과: 예약 N · 확정 N · 취소 N(톤 낮춤)
  - **확정률 = 색 배경**(초록 좋음 / 노랑 보통 / 빨강 주의)로 훑을 때 색으로 판단
- 우측 **최근 소개 날짜 뱃지**(예: "최근 소개 2025-04") — 리스트만 봐도 최근 여부 파악
- **채널은 코드 아님 이름으로**(HT→호텔이야). 괄호식 부가표시 배제.
- 개별 노출·마지막 날짜 세부는 목록에서 빼고 → "눌러서 상세".

### 채널 코드→이름 (확정)
HT=호텔이야 · HG=호텔이곳 · TW=여행능력자들 · JP=ホテルだ · ZH=世界就是家 · KT=Koreahotelguide · VN=reviewkhachsan · legacy=과거 누적

## 4) 눌러서 상세 → 노출 이력 (종합의 근거)
- 노출 이력 표: 게시일 · 영상 · 채널 · 순위 · 예약 (개별 노출 모두 쌓임, 여기서 마지막 소개일도 확인)

## 5) 노출 이력 각 줄 클릭 → 그 노출 세부 펼침 (2단)
- 세부 요약 카드: 조회수·클릭(=연동 후) · 이 영상 예약 · 확정 · **예약금액**(초록, 이 영상이 번 돈) · 수수료(대표님만)
- 맥락: 게시일(정확 날짜) · 순위(N곳 중 X위) · **유튜브 영상 보기 링크**
- **이 콘텐츠로 잡힌 예약 리스트 = 매니저 페이지 컬럼 그대로**:
  예약번호 · 예약일 · 체크인 · 체크아웃 · 박 · 국가 · **금액** · 상태 (+ **수수료 = admin(대표님)만**, 매니저·에디터 숨김)
  - 소스 재사용: `v_manager_recent_bookings` / `/api/hotel-bookings` (D-053 원칙: 매니저 형태 미러링 + 수수료 플래그)
  - 취소 건 금액 회색.

## 6) 라이브 반영 원칙 (중요)
- **기존 것 삭제 금지.** 기존 상세(노출 이력·개별 예약 등)는 그대로 두고 새 구조를 병행/얹으며, 하나씩 불러와 대조하며 맞춘다.
- 실데이터 우선순위: 예약·확정·취소·확정률·금액·순위·날짜·채널 = 지금 실데이터 / 조회·클릭 = 연동 후.

## 7) 유료 계약($200) 카드 — 상세 화면 (보류 유지)
- PAID(초록: 인보이스·결제일·노출 시작·6개월 보장 D-day·채널·계약 후 예약) / 무료(회색 "소개만·유료 계약 없음").
- **선결: 아고다↔계약 호텔 매칭**(hotels ↔ bookings_agoda). 매칭 전엔 대부분 무료로 뜸 → 매칭 후 착수(과거에도 보류 권고).


---

# 확정 v3.1 (2026-07-13) — 상세 재정리 (예약·취소 타이밍 전략)

## 상세 순서 (확정)
① 노출 이력 → ② 성과 종합 → ③ 예약·취소 타이밍(리드타임) → ④ 개별 예약(매니저 컬럼)
- **기존 "④ 예약·방문·취소·노쇼" 블록 삭제** — 성과 종합에 흡수(방문·취소·노쇼는 성과 종합에 작게 표기).

## 목적 (대표님 설명)
예약이 보통 언제 이뤄지나 + 일찍 예약한 게 언제/얼마나 취소되나 전략 + 호텔 깊이 분석 + 유료($200) 호텔에 상세 설명. 아고다 취소 데이터 활용가치(D-062 ⑤) 접목.

## 데이터 실측 (2026-07-13, 7,169행)
- **취소 타입 존재**: Cancelled By Customer 3,366 · Cancelled, Other 36 · Departed(방문) 3,756 · Charged 8 · Confirmed 3. → 취소를 **고객취소/기타취소**로 구분 표기.
- **있는 값**: 예약일·체크인·체크아웃·박·국가·금액·기기(device_type)·객실수. (금액·국가·기기·박 = 정상건만, 취소건은 비어 있음)
- **없는 값(전부 null)**: room_type(룸타입)·num_adults/children(인원) → **화면에서 제외**("성인0" 제거).
- **취소일 없음** → 진짜 취소 타이밍(체크인 D-day) 불가. raw_row_data도 취소건 null. 아고다 파일에 취소일 들어오면 승격.

## ③ 예약·취소 타이밍 = 리드타임 기반
- 리드타임=체크인−예약일. 3구간(임박0–7·근접8–30·먼31+) 확정률 막대. "며칠 전 예약이 잘 깨지나" → TOP3 판단.

## ④ 개별 예약 = 매니저 컬럼 (확정)
예약번호 · 예약일 · 체크인 · 체크아웃 · 박 · 국가 · **기기(📱/💻)** · 금액 · 상태(방문/고객취소/기타취소) (+ **수수료 = admin만**).
- 취소건 금액·국가·기기 "—"(아고다 미제공). 소스 v_manager 미러링(D-053).

## 디테일 축 4 (실데이터, 대표님 승인)
리드타임(언제) · 취소 타입(고객/기타) · 기기(모바일/PC) · 국가·박수. 룸타입·인원 제외.

## API 반영 (content-hotels.js)
개별예약에 reservation_no·device·취소타입(status) 추가, 취소건 amount/commission=null, room_type·adults·children 제거.
